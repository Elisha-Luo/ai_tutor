# ============================================================
# 这是一个「模板」文件，里面放的全是假值，可以安全地提交到 GitHub。
#
# 怎么用：
#   1. 把这个文件复制一份，改名叫 .env（注意前面有个点，后面没有 .example）
#   2. 把等号后面的值换成你自己的真实密钥
#   3. 重新启动 python app.py
#
# .env 已经被 .gitignore 忽略，绝不会被推到 GitHub —— 这才是放密钥的地方。
# ============================================================


# DeepSeek 的 API 密钥。在 https://platform.deepseek.com 申请，形如 sk- 开头的一串字符。
DEEPSEEK_API_KEY=sk-在这里填你自己的密钥


# Flask 用来给浏览器 cookie 签名的密钥，作用是防止别人伪造 cookie 冒充成别的会话。
#
# 【必须换掉】不要直接用下面这串示例值。生成一个属于你自己的随机串：
#     python -c "import secrets; print(secrets.token_hex(32))"
# 把打印出来的那串字符粘到等号后面即可。
FLASK_SECRET_KEY=change-me-run-the-command-above-to-generate-your-own
