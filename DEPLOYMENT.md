# 部署到 Railway（免费版）

这份文档写给第一次做部署的人。全程用「开一家小店」来打比方。

> **本轮说明**：这份文档只是**说明书**。写作时**没有创建任何 Railway 服务、没有连接任何账户、没有产生任何费用**。
> 真正去下单开店，需要你另外授权。

---

## 〇、先搞清楚：本地和线上是两回事

| | 本地开发 | Railway 生产 |
| --- | --- | --- |
| 谁来启动程序 | 你自己敲 `python app.py` | Railway 按启动命令拉起 **Gunicorn** |
| 用的服务器 | Flask 自带的**开发服务器** | **Gunicorn**（专门给生产用的） |
| 端口 | 固定 `5000` | 平台给一个端口，放在 `$PORT` 变量里 |
| 数据存哪 | 项目文件夹里的 `chat.db` | 挂载的**持久卷**上的 `/data/chat.db` |
| 别人能访问吗 | 只有你自己（`127.0.0.1`） | 有公网地址，谁都能打开 |

**最关键的一句**：本地能跑 ≠ 线上能跑。这两条路的服务器、端口、数据位置全都不同。
所以本地测好之后，线上第一次部署仍然要认真按下面每一步做。

---

## 一、这家「店」里各个东西对应什么

| 厨房里的东西 | 对应到项目 |
| --- | --- |
| 菜谱 | `retriever.py`（找食材）+ `rag.py`（做菜并核对食材来源） |
| 食材柜 | `knowledge_base/` 里的 3 份 Markdown 资料 |
| 厨师 | DeepSeek（通过网络请的外援厨师） |
| 前台 | `app.py` + `templates/index.html` |
| 点菜记录本 | SQLite 数据库（`chat.db`） |
| 店长（负责开门、盯着厨师别把厨房烧了） | **Gunicorn** |
| 店面的门牌号 | 平台给的 `$PORT` |
| 新店开门前的**验收员**（检查合格了，才让客人进来） | **健康检查 `/health`** |
| 库房（货放这儿才不会一关门就没） | **持久卷**，挂在 `/data` |

---

## 二、为什么要挂「持久卷」——这是最容易踩的坑

Railway 的容器**每重新部署一次就是全新的一个**，里面写的东西**全部消失**。

所以如果你什么都不做，会出现这种情况：

```
用户聊了一下午  →  你改了一行代码重新部署  →  聊天记录全没了
```

这不是 bug，是容器的正常行为。解决办法就是**挂一个持久卷**（可以理解成「租一个不会随店面翻新而清空的库房」），把它挂到 `/data`，然后把数据库指过去。

代码里已经写死了一条规矩（`_check_db_path`）：

> **如果 `/data` 这个目录不存在，程序会直接拒绝启动，而不是偷偷退回项目目录建一个数据库。**

为什么这么「不近人情」？因为退回项目目录的话，应用会**一切正常地跑起来**，但数据其实写在随时会消失的地方。
这种「看起来正常、其实没持久化」的假成功，会让你在真正丢了数据之后才发现。**宁可开不起来，也不要假成功。**

---

## 三、在 Railway 上要设置的东西

### 3.1 环境变量

在 Railway 项目的 **Variables** 页面里，一条一条加：

| 变量名 | 填什么 | 为什么 |
| --- | --- | --- |
| `DEEPSEEK_API_KEY` | 你的 DeepSeek 密钥 | 请「外援厨师」的入场券 |
| `FLASK_SECRET_KEY` | 一串够长够随机的字符 | 给浏览器 cookie 签名，防别人伪造身份 |
| `CHAT_DB_PATH` | `/data/chat.db` | 把点菜记录本放进库房，而不是放在随时会清的柜台 |
| `MAX_QUESTION_LENGTH` | `500` | 一个问题最多 500 字，太长的直接挡掉 |
| `DAILY_API_LIMIT` | `50` | 全站每天最多请 50 次外援厨师，保护你的钱包 |

`FLASK_SECRET_KEY` 的生成方法（在本地终端跑）：

```powershell
python -c "import secrets; print(secrets.token_hex(32))"
```

把打印出来那串粘进去。**不要用示例值，也不要用你自己想的一句话**——那等于没锁门。

> **⚠️ 密钥只填在 Railway 的 Variables 里，绝不要写进代码或提交到 GitHub。**
> 代码里所有密钥都从环境变量读，仓库里一个都没有。

### 3.2 挂载持久卷

在 Railway 项目的 **Volumes** 里新建一个卷，**挂载路径填 `/data`**。

挂好之后，`CHAT_DB_PATH=/data/chat.db` 才有意义——那个 `/data` 就是这块卷。

### 3.3 启动命令

在 **Settings → Deploy** 的启动命令里填：

```
gunicorn --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 120 app:app
```

拆开看每一段的含义：

| 这一段 | 意思 |
| --- | --- |
| `--bind 0.0.0.0:$PORT` | 监听平台给的那个端口。写死成 5000 会连不上 |
| `--workers 1` | **只开一个工作进程**（下面专门解释为什么） |
| `--threads 4` | 一个进程里开 4 条线程，能同时应付几个请求 |
| `--timeout 120` | 单个请求最多等 120 秒（问模型可能要好几秒） |
| `app:app` | 意思是「打开 `app.py`，里面那个叫 `app` 的对象」 |

### 3.4 健康检查

在 **Settings → Deploy → Healthcheck Path** 里填：

```
/health
```

### 它到底在什么时候被调用（这一节很重要，很多资料都写错了）

**它不是「持续监控」，而是「新店开门前的验收」。**

Railway 的机制是这样的：

1. 你部署了一个新版本，Railway 先把新版本启动起来
2. Railway 会在健康检查的超时时间内**反复请求** `/health`，直到收到 2xx
3. 收到了 2xx（就是 200 这类成功码），Railway 才**把流量切给新版本**
4. 如果直到超时仍未成功，这次新部署会被**标记为失败**
5. **部署完成之后，Railway 就不再访问这个地址了**

所以要专门澄清三件容易搞混的事：

| 常见误解 | 实际情况 |
| --- | --- |
| 「Railway 会一直盯着 `/health`」 | ❌ **只在部署新版本时**，而且是反复请求直到成功或超时 |
| 「线上 `/health` 连续失败会自动重启服务」 | ❌ **不会**——部署完成后它就不再看了 |
| 「进程挂了会自动重启」 | ✅ **会**——但那是**另一套机制**（**Restart Policy**），和 `/health` 没有关系 |

**如果你想要「服务挂了自动告警」这类持续可用性监控，得另外配监控工具**（比如外部的定时拨测服务）。
本项目**当前没有配置**这类监控。

### ⚠️ 另一个重要限制：重新部署会**短暂停机**

**因为这个项目挂着一个持久卷，重新部署时会出现短暂停机——即使配置了健康检查，也不能保证零停机。**

原因在于：Railway 要避免**两个部署同时挂载同一个卷**——那会让新旧两边的程序同时读写同一份数据，
是必须避免的危险情况。所以它会先让旧部署停下来，再让新部署接管这个卷。
**这个「交接」的过程，就是你会感受到的停机窗口。**

**这是当前「SQLite + 单卷」架构的已知限制，不是哪里配置错了：**

| 你想要的效果 | 现在能做到吗 |
| --- | --- |
| 重新部署时**数据不丢** | ✅ 能——这正是持久卷的作用 |
| 重新部署时**零停机** | ❌ **不能**——单卷决定了必须先交接 |

**健康检查解决不了这个问题**：它管的是「新版本有没有准备好」，管不了「卷能不能被两个部署同时用」。

要真正做到零停机，需要把数据从单文件 SQLite 换成支持多实例并发访问的数据库（比如 PostgreSQL），
并相应地去掉「单卷」这个约束。**那是另一个阶段的事，不在本轮范围内。**

### 那 `/health` 还有用吗？有，而且不少

- **部署时的自动验收**：通过这个端点确认 Flask 起没起来、SQLite 能不能打开
- **手工排障**：浏览器直接打开 `你的地址/health`，立刻知道数据库通不通
- **不花钱**：它不调用 DeepSeek，随便点

它做了这些，也**刻意没做**这些：

- ✅ 真的执行一次 `SELECT 1`，确认数据库能读——**进程活着不等于服务可用**
- ✅ 不调用检索、不调用 DeepSeek、不读写聊天记录、不创建用户会话
- ✅ 失败时只返回一句固定的话，不暴露数据库路径或异常内容

### 3.5 只开一个实例、一个 worker

在 **Settings** 里把实例数保持为 **1**。

---

## 四、为什么必须是「单 worker、单实例」

**这是本项目最重要的一条部署约束，不要为了「性能」改成多个。**

原因有三个，缺一不可：

1. **SQLite 是单文件数据库。**
   它支持多进程读写，但写操作是串行的。真开 4 个 worker，写冲突会变成常态，日志里会开始出现 `database is locked`。

2. **应用里有一把「每次只处理一个问题」的进程内锁。**
   这把锁（`_lock`）只在一个进程里有效。开 4 个 worker 就等于有 4 把互不相干的锁——防重复提交的功能直接失效。

3. **所有 worker 共用同一个持久卷上的数据库文件。**
   多进程抢同一个 SQLite 文件，是最容易出现「偶发、难复现」的数据问题的场景。

> **结论**：`--workers 1` 不是「先凑合」，而是这个架构下的正确选择。
> 真要提高并发，应该先把数据库换成 PostgreSQL，那是另一个阶段的事。

启动命令里用 `--threads 4` 来提升并发：**多线程共享同一个进程**，所以那把锁仍然是唯一的、有效的。

---

## 五、免费版的两个坑，提前知道

### 5.1 额度很小

Railway Free 每月给少量资源额度（以官方页面为准）。这个应用很轻，正常演示够用，但要注意：

- **持久卷只有约 0.5 GB。** 聊天记录是纯文本，很小，够用很久；但它是**有限**的——不是无限的。
- **流量和运行时长都算在额度里。** 没人访问时建议让它休眠，别一直开着。

### 5.2 Limited Trial 账户可能连不上 DeepSeek

处于 **Limited Trial** 状态的账户，**出站网络可能被限制**。表现是：

```
应用能打开，页面正常，但每次提问都返回「这次没能处理你的问题」
```

代码里对这种失败已经做了安全处理（不会崩、不会泄露异常），但**功能上就是不可用**。

**怎么判断是不是这个问题**：看日志里有没有 `internal_error` 或模型调用相关的记录。如果是网络被限制，
需要用非 Limited Trial 的账户，或者等账户升级。

> 这一条不是代码能解决的，属于账户层面的限制。提前知道，免得以为是代码写错了。

---

## 六、部署之后怎么确认「数据库真的在 /data 上」

这一步不能省。**没确认过就永远不知道数据到底存哪了。**

### 方法一：看启动日志

应用启动时会打一行：

```
app_start debug=False db_file=/data/chat.db
```

**看到 `db_file=` 后面是 `/data/chat.db`，就说明路径对了。**

如果看到的是项目目录下的路径（比如 `/app/chat.db`），说明 `CHAT_DB_PATH` 没设对——
这时候数据是**不持久**的，重新部署就没了。

### 方法二：故意重启一次验证

1. 在网页上问一个问题（记录会写进数据库）
2. 在 Railway 上**重新部署一次**（Redeploy）
3. 刷新网页——**如果历史还在，说明持久卷生效了**

第 3 步看到记录没了，就是没挂对，回去检查 3.2。

### 方法三：健康检查

浏览器直接打开 `你的地址/health`，应该看到：

```json
{"status": "ok"}
```

如果看到 `{"status": "unhealthy"}` 并且是 503，说明**数据库打不开**——通常是持久卷没挂好。

---

## 七、怎么看日志（以及什么绝对不能贴出来）

在 Railway 项目的 **Deployments → 点开某次部署 → Logs** 里看。

日志里会有这类结构化记录：

```
2026-09-22 10:00:00,000 INFO app_start debug=False db_file=/data/chat.db
2026-09-22 10:00:05,120 INFO question_received question_length=23
2026-09-22 10:00:08,400 INFO rag_decision decision=answer citation_count=1 elapsed_ms=3120
```

**每一行都是「事件名 + 几个字段」，故意设计成这样**：机器能统计，人也能一眼看懂。

### ⚠️ 分享日志之前请确认

日志里**故意不记**这些东西（代码里有白名单强制保证）：

- ❌ 用户问的问题原文（只记 `question_length`，也就是长度）
- ❌ AI 回答的原文
- ❌ 任何密钥
- ❌ 原始 session_id / cookie
- ❌ 完整的模型提示词
- ❌ 异常原文（只记异常**类型**）

所以日志**本身是可以贴出来求助的**。但为了稳妥，贴之前还是扫一眼有没有你自己不小心打印的别的东西。

> 字段白名单在 `app.py` 的 `_SAFE_LOG_FIELDS`。**不在名单里的字段名会被自动丢掉**——
> 这样就算以后有人手滑写了 `log_event("x", question=用户原话)`，正文也进不了日志。
> 这是结构性保证，不靠人自觉。

---

## 八、每日额度是干什么的（以及它不是什么）

### 它是什么

全站每天最多调用 `DAILY_API_LIMIT`（默认 50）次模型，按 **UTC 日期**计算。用完了就返回：

> 今天的使用额度已经用完了，请明天再来。

额度记录存在**数据库的 `api_usage` 表**里，所以**重启应用不会归零**。

### 它在什么时候扣

**只在「确定马上就要调用模型」的那一刻扣一次。** 具体来说：

| 情况 | 扣额度吗 |
| --- | --- |
| 空问题 | ❌ 不扣 |
| 问题太长被挡 | ❌ 不扣 |
| 健康检查 `/health` | ❌ 不扣 |
| 没检索到资料（不调模型） | ❌ 不扣 |
| **马上要调模型** | ✅ **扣一次** |
| 模型调用**失败**了 | ✅ **照样扣** |

最后一条值得多说一句：**失败也算**。因为那一次**已经真实发出过外部请求**了，
风险已经产生。要是失败不算数，别人就能靠不停制造失败来「免费重试」。

### ⚠️ 它不是什么（诚实说明）

**这是一个「保护演示费用」的全站总额度，不是完整的生产级限流系统。**

它做不到这些：

- 不区分用户（没有登录，所以只能全站一个总数）
- 不做按 IP 的频率限制
- 不做验证码、不做防机器人
- 不防「一个人把全站额度用完」——50 次用完就大家一起等明天

**一句话：它是防止演示期间账单失控的保险丝，不是防滥用的大门。** 真要对外提供服务，这一块需要重新设计。

---

## 九、回滚

### 9.1 回滚代码

在 Railway 的 **Deployments** 页面，找到上一个正常的部署，点 **Redeploy**（或者 Rollback）。

平台会把代码换回那个版本。**这是平台层面的一键操作，不需要你改任何代码。**

### 9.2 ⚠️ 回滚代码 **不会** 回滚数据库

这一条必须说清楚：

> **代码回滚了，`/data/chat.db` 里的数据还在，而且可能是「新版本写的、旧版本不认识」的状态。**

具体来说：

- 数据**不会丢**——持久卷上的文件不会因为回滚代码而改变
- 但如果新版本给数据库**加了表或加了列**，回滚后的旧代码可能不认识它们
- 反过来，旧代码写进去的数据，新版本一般能正常读（本项目只做加法，不做删改）

**所以回滚前要想清楚：**

| 回滚原因 | 数据要不要一起处理 |
| --- | --- |
| 只是代码写错了 | 数据不用动，直接回滚代码即可 |
| 数据库结构改坏了 | 数据也要一起处理——**先给持久卷做一次备份**再动手 |

### 9.3 怎么给数据做备份（以及什么不算备份）

Railway 对挂载卷提供了**备份功能**，走官方流程：

1. 在服务的 **Backups** 页面，为挂载的卷**创建一个手动备份**
2. 也可以设置**备份计划**——每日、每周或每月自动备份
3. **恢复**备份时，Railway 会**创建一个恢复后的新卷并挂载它**；
   旧卷会保留下来，但**解除挂载**（不会自动删掉，方便你确认没问题之后再清理）
4. 备份按**增量占用的存储**计费

> ⚠️ **不要把 `cp /data/chat.db /data/backup.db` 当成备份方案。**
>
> 那只是**同一个卷里的一份拷贝**。卷本身被删除、损坏或挂载出问题时，
> **两份会一起没**。它挡得住「手滑删了某张表」，挡不住「整个卷没了」——
> 而后者才是真正需要备份来兜底的场景。
>
> 一句话：**同一个抽屉里放两份，不等于备份。**

> **本轮说明**：写这份文档时**没有实际创建任何备份，也没有产生任何存储费用**。
> 上面写的是操作步骤，需要你自己在控制台里点。

> 本项目的数据库结构是「只增不改」的（`CREATE TABLE IF NOT EXISTS`），
> 所以大多数情况下回滚代码是安全的。但**知道这条规则存在**，
> 比记住「本项目没事」更重要——换一个项目就不一定了。

---

## 十、这份文档没做的事

**诚实记录**：

- ✅ 只写了文档和代码
- ❌ **没有创建 Railway 账户**
- ❌ **没有创建任何 Railway 服务**
- ❌ **没有连接 GitHub**
- ❌ **没有部署到公网**
- ❌ **没有产生任何费用**
- ❌ **没有新增 `railway.toml` 或 `railway.json`**

最后一条解释一下：Railway 的 **Config as Code**（也就是把部署参数写成配置文件提交到仓库）
**已经进入弃用期**（旧格式只支持到 2026-12-01）。所以本阶段**不新增这类文件**，
部署参数一律在上面的控制台页面里配置，并且记录在这份文档里。

---

## 十一、上线前的检查清单

按顺序过一遍，每一项都确认了再上线：

- [ ] `DEEPSEEK_API_KEY` 已设为真实密钥
- [ ] `FLASK_SECRET_KEY` 已设为一串新的随机字符（不是示例值）
- [ ] `CHAT_DB_PATH=/data/chat.db` 已设置
- [ ] `MAX_QUESTION_LENGTH=500` 已设置
- [ ] `DAILY_API_LIMIT=50` 已设置
- [ ] **持久卷已挂载到 `/data`**
- [ ] 启动命令是 `gunicorn --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 120 app:app`
- [ ] 健康检查路径是 `/health`
- [ ] 实例数 = 1
- [ ] 部署后打开 `/health` 看到 `{"status":"ok"}`
- [ ] 启动日志里 `db_file=/data/chat.db`
- [ ] **重新部署一次，确认聊天记录还在**（这一步最能说明问题）
- [ ] 问一个问题，确认页面上显示了「资料来源」
- [ ] 看一眼日志，确认没有异常

---

## 十二、出问题了怎么办

| 现象 | 最可能的原因 | 怎么办 |
| --- | --- | --- |
| 部署失败，日志里有「数据库所在目录不存在」 | 持久卷没挂，或 `CHAT_DB_PATH` 写错 | 检查持久卷挂载路径是不是 `/data` |
| **部署时** `/health` 返回 503，新版本一直切不上去 | 数据库打不开 | 检查持久卷是不是挂到了 `/data` |
| 手工打开 `/health` 返回 503 | 数据库打不开 | 同上 |
| 服务进程崩了但没有自动拉起 | 没配重启策略 | 这是 **Restart Policy** 管的，和 `/health` 无关——去服务设置里配置 |
| 页面能开，但提问总是失败 | 出站网络被限制（Limited Trial） | 见第 5.2 节 |
| 提问返回「今天的使用额度已经用完了」 | 当天 50 次用完了 | 等第二天（UTC 时间），或调大 `DAILY_API_LIMIT` |
| 提问返回「你问的问题太长了」 | 超过 500 字 | 精简问题，或调大 `MAX_QUESTION_LENGTH` |
| 重新部署后聊天记录没了 | 持久卷没生效 | **重点检查这一条**，见第六节 |
| 日志里出现 `database is locked` | 有人把 worker 调成了多个 | 改回 `--workers 1` |
