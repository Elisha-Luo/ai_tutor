# ============================================================
# .gitignore 的意思是：这个文件里列出的东西，git 不要管
# 也就是「不要提交、不要推到 GitHub」
# ============================================================

# Python 运行时自动生成的缓存文件夹。
# 你没写过它，是 Python 自己建的（放着编译过的中间文件，好让下次启动快一点），
# 但它是你机器上生成的东西，别人不需要，不该进仓库。
__pycache__/

# 虚拟环境（如果你以后建的话）
venv/
.venv/

# 编辑器的配置文件，属于个人习惯，不该提交
.vscode/
.idea/

# 【重要】真实密钥文件。.env.example 是模板（可以提交），.env 是真货（绝不能提交）。
.env
*.key

# 聊天记录数据库。里面存着真实对话内容，属于用户数据，不该进仓库。
# 顺带说明：数据库里【不存】API 密钥，密钥只在环境变量里。
chat.db

# 数据库跑起来时可能产生的临时文件
chat.db-journal
chat.db-wal
chat.db-shm
