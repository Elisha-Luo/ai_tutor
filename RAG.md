# 生成与引用层（RAG 第二步）

这个文档解释 `rag.py` 在做什么、为什么这么设计，尤其是**为什么它的绝大部分代码不是用来问模型的，而是用来不信模型的**。

---

## 一、retriever.py 和 rag.py 的分工

RAG = Retrieval（检索）+ Augmented Generation（增强生成）。这个项目把它拆成了两个文件，一个管前半段，一个管后半段：

| | `retriever.py` | `rag.py` |
| --- | --- | --- |
| 干什么 | **找资料**——把问题变成「最相关的几段」 | **用资料**——让模型照着这几段回答 |
| 输入 | 一个用户问题 | 问题 + 已经检索到的片段 + 模型客户端 |
| 输出 | 几个片段（带 source / heading / text） | 一个可检查的回答结果（带引用） |
| 调模型吗 | ❌ 纯本地文字匹配 | ✅ 调模型 |
| 能离线跑吗 | ✅ 完全能 | ❌ 需要模型（但测试用假客户端，也能离线跑） |
| 出错会怎样 | 返回空列表，不硬凑 | 安全降级成「证据不足」，不放行 |

一句话记住：**`retriever.py` 负责「找到」，`rag.py` 负责「用好，并且证明自己用对了」。**

```
用户问题
   │
   ▼
┌──────────────┐   几段资料   ┌──────────┐   带引用的回答
│ retriever.py │ ──────────▶ │  rag.py  │ ───────────────▶  用户
└──────────────┘             └──────────┘
   纯本地，不花钱              调模型，但有六道关在盯着它
```

---

## 二、返回格式

`rag.py` 永远返回同一个形状，一个键不多、一个键不少：

```python
{
  "decision": "answer" | "refuse" | "insufficient_evidence",
  "answer":   "给用户看的中文回答",
  "citations": [{"source": "文件名", "heading": "二级标题"}]
}
```

**为什么格式必须固定？** 因为调用方（以后是网页）要能安全地渲染它。如果有时有 `citations`、有时没有，每一个渲染的地方都得写一堆防御代码，早晚漏一个。固定形状意味着**只有一种情况需要处理**。

### 三种 decision 是什么意思

| decision | 什么时候出现 | 给用户什么 |
| --- | --- | --- |
| **answer** | 资料能**完整支持**一个回答 | 真回答 + **至少一条**引用 |
| **refuse** | 资料里**完全没有**相关内容 | 明确说「资料里没有」，不给猜测 |
| **insufficient_evidence** | 资料**沾边但不够**，说不清楚 | 说明「信息不足」，不硬答 |

---

## 三、六道关：拿回答案之后要过什么

这是 `rag.py` 的核心。模型返回的东西**默认不可信**，要一层层查：

| 关口 | 检查什么 | 不过怎么办 |
| --- | --- | --- |
| 1 | chunks 是不是空的 | 空 → 直接拒答，**根本不调模型** |
| 2 | 调模型有没有抛异常 | 有 → 降级为 insufficient_evidence |
| 3 | 返回的是不是合法 JSON | 不是 → 降级 |
| 4 | decision 是不是那三个值之一 | 不是 → 降级 |
| 5 | **每条引用是不是真的在本次传入的片段里** | 有一条不对 → **整份否掉** |
| 6 | answer 有没有引用？非 answer 有没有多余引用？ | 不对 → 降级 |

**第 5 关是整层的关键**，下一节专门讲。

另外有一条贯穿始终的原则：**任何降级路径都不把模型的原始错误抛给用户。** 网络异常、堆栈信息、内部细节——用户只需要知道「这次没能给出可靠回答」。测试里专门有一条盯着这件事。

---

## 四、为什么 citation 白名单能挡住「编出来的引用」

### 先说清楚危险在哪

用户看到一段回答，末尾写着：

> 来源：`grammar_present_perfect.md` · 现在完成时的用法

**绝大多数人到这里就不查了。** 一个看起来有出处的答案，比一个没有出处的答案更容易让人相信——**哪怕那个出处是编的**。

这就是「引用幻觉」：模型不一定编内容，它可能内容大致对，但**顺手编一个看起来很像的来源**。文件名是真的、标题是编的；或者文件名标题都真，但那段资料这次根本没检索出来。

所以问题的本质不是「模型会不会编」，而是**「我们有没有办法发现它编了」**。

### 办法：白名单

`rag.py` 在调模型之前，先把**这次真实传进去的**每一段的 `(source, heading)` 收进一个集合：

```python
allowed = {(c["source"], c["heading"]) for c in chunks}
```

模型返回后，每一条 citation 都要拿这个集合对照：

```python
if key not in allowed:
    return None      # 引用了没给它的来源 —— 编的
```

**为什么这招有效：** 因为白名单是**代码自己算出来的**，不是模型说的。模型可以编任何文件名、任何标题，但它编出来的东西**不可能恰好出现在一个它没见过的集合里**。这是一个无法绕过的检查——除非它老老实实引用真正传给它的那几段。

### 一条坏引用，整份否掉

代码里做的不是「把坏的那条删掉、留下回答」，而是**整份降级**。

理由：一条引用是编的，说明这个回答整体就不可信了——它很可能连内容也是编的。只删坏引用、保留回答，等于**把一段没有依据的话包装成「有引用」的样子**，那比直接拒绝危险得多。

同理，`answer` 没有引用也直接否掉：**说完话不给出去，就是不可查证**，那正是 RAG 要解决的问题。

---

## 五、refuse 和 insufficient_evidence 的区别

这两个都是「没给出回答」，但**原因完全不同**，对用户的意义也不同：

| | refuse | insufficient_evidence |
| --- | --- | --- |
| 意思 | 资料里**根本没有**这方面内容 | 资料**提到了**，但**不够完整** |
| 例子 | 问「课程多少钱」——资料完全没有价格信息 | 问「现在完成进行时和现在完成时的区别」——资料只讲了现在完成时，没讲进行时，比不了 |
| 对用户的价值 | 「别在这找了，换个地方问」 | 「找对地方了，但资料不够，建议查更完整的」 |
| 什么时候触发 | chunks 为空；或模型判断完全无关 | 模型判断信息不足；或**任何校验没过** |

**为什么要分这两种，而不是合成一个「不知道」？**

因为它们的**下一步动作不一样**。第一种，用户该去别处找答案；第二种，用户该去补充资料、或者找老师确认。混成一个「不知道」，用户就不知道该往哪走了。

**还有一层：** `insufficient_evidence` 同时是这一层的**默认安全出口**。模型返回坏 JSON、编了来源、漏了引用、直接报错——这些情况下我们**不知道**该算 refuse 还是 answer，所以统一降级到 `insufficient_evidence`。它是那个「不知道该怎么办时的正确答案」。

---

## 六、还没接网页，为什么这是刻意的

`rag.py` 现在**完全独立**：它不 import `app.py`，不被任何路由调用，用户从网页上还看不到它。

这不是没做完，是**故意的分步验证**：

**1. 每一层要能单独测。**
`rag.py` 的测试完全不需要 Flask、不需要数据库、不需要网页。塞一个假客户端进去，就能把「模型编来源」「模型返回坏 JSON」这些**现实中很难复现的情况**一条条造出来跑。如果一上来就接进网页，这些情况你得靠手动点页面去碰运气。

**2. 失败要能被定位到具体哪一层。**
用户拿到一个烂答案时，问题可能出在**检索**（没找对资料），也可能出在**生成**（资料对，但模型瞎答）。两层分开、各自有测试，才分得清。一旦糊在一起，就只能靠猜。

**3. 网页是最后一层，也是最难改的一层。**
网页牵涉 session、数据库、用户界面。**在下面两层稳定之前接网页，等于在一个会晃的地基上盖房子**——每次改检索逻辑，都得连网页一起回归测试。

**4. 顺序和路线一致。**
RAG 的下一步是「拿 33 道题跑完整评测」：既看检索对不对，也看拒答和不编造对不对。**这个评测必须在命令行跑**——因为一次评测要调几十上百次模型，做成网页就要引入后台任务队列，那是现在完全不需要的复杂度。

所以正确的顺序是：

```
retriever.py（✅ 已完成，17/17 + 3/3 通过）
      ↓
rag.py（✅ 本轮完成，44 个测试）
      ↓
命令行跑 33 道题的完整评测   ← 下一步
      ↓
接进网页和 chat.db           ← 再下一步
```

---

## 七、这一层现在能证明什么

| 项目 | 结果 |
| --- | --- |
| chunks 为空时不调用模型，直接拒答 | ✅ |
| 合法回答 + 合法引用能通过 | ✅ |
| 模型编造文件名 → 拦截 | ✅ |
| 模型编造标题 → 拦截 | ✅ |
| 引用真实存在但本次没传入的片段 → 拦截 | ✅ |
| answer 没有引用 → 拦截 | ✅ |
| refuse / insufficient 带引用 → 拦截 | ✅ |
| 坏 JSON、模型异常、返回 None → 安全降级不崩 | ✅ |
| 模型异常信息不泄露给用户 | ✅ |
| 返回格式永远固定、永远可 JSON 序列化 | ✅ |

**能证明的：**

- 模型**无法**引用一个没传给它的来源——这是结构性的，不靠模型自觉
- 任何异常输入都不会让程序崩，也不会放一条没依据的回答过去
- 这一层不碰密钥、不依赖网络库，客户端从外面传进来

**还不能证明的（重要）：**

- ❌ **不能证明回答内容是对的。** 引用能验证「出处存在」，但验证不了「这段内容真的支持这个回答」——那需要人来判断，或者更复杂的校验。
- ❌ **不能证明检索是对的。** 这一层完全信任传进来的 chunks。检索找错了资料，它会认真地基于错误资料作答。
- ❌ **不能证明拒答能力在真实问题上也成立。** 目前只测了「模型返回 refuse 时我们接得住」，还没拿 33 道题里的 13 道拒答题实测过模型的**判断**准不准。那要等完整评测。
- ❌ **白名单挡不住「引用对、内容错」。** 模型可以引一段真的资料，却说一句这段资料不支持的话。这是这一层的天花板。

---

## 八、代码在哪 / 怎么跑

| 文件 | 内容 |
| --- | --- |
| `rag.py` | 生成与引用层本体。提示词、六道校验、返回格式 |
| `test_rag.py` | 44 个测试，全部用假客户端，离线运行 |
| `retriever.py` | 上一层：本地检索 |
| `evals/rag_cases.json` | 33 道验收题 |

跑测试：

```powershell
cd C:\Claude-Code\ai_tutor
python -m unittest test_app test_retriever test_rag -v
```

手动接起来用（客户端由调用方提供，`rag.py` 自己不碰密钥）：

```python
from retriever import retrieve
from rag import generate_answer

chunks = retrieve("since 和 for 有什么区别？", top_k=3)
result = generate_answer("since 和 for 有什么区别？", chunks, client, model)
# result["decision"] / result["answer"] / result["citations"]
```

---

## 九、下一步

1. 命令行跑 33 道题的完整评测：检索 + 生成 + 拒答，三项一起看
2. 重点盯 13 道拒答题——看模型会不会「资料沾边就硬答」
3. 两层都稳了，再接进 `app.py` 和 `chat.db`
