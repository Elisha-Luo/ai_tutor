# -*- coding: utf-8 -*-
# 上面这行告诉 Python：这个文件用 UTF-8 编码，中文才不会乱码

# =====================================================================
# 让 evals/ 成为一个「包」（package）
#
# 【为什么需要这个文件的空壳】
# 本轮的评测器要用这种方式运行：
#     python -m evals.run_rag_eval --dry-run
#
# 其中 `-m` 表示「把后面那串当成模块路径来找」。Python 要在 evals/ 目录里
# 看到这个 __init__.py，才会把 evals 认成一个可导入的包。
#
# 【为什么留空不写代码】
# 它只是个标记文件。真正的逻辑在 run_rag_eval.py 里。
# 保持它为空，是为了让「这是个包」这件事本身一目了然。
# =====================================================================
