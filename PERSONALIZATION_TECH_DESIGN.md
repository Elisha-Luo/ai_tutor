# 个性化功能 · 技术设计

> **本文档只写设计，不含代码。**
> 配套：`PRODUCT_DISCOVERY.md`（发现）、`PRODUCT_REQUIREMENTS.md`（需求）、`PERSONALIZATION_EVALS.md`（验收）。
>
> **不含任何真实邀请码、真实用户身份信息。**
>
> 状态：**设计已定稿，尚未开发。**

---

## 1. 当前系统结构与必须保留的能力

### 现在长什么样

```
浏览器
   │
   ▼
app.py (Flask)
   ├── 会话隔离（cookie 里的随机 session id）
   ├── 单进程锁（防重复提交）
   ├── 每日额度（api_usage 表，UTC 日期）
   ├── 日志（字段白名单，不记正文）
   ├── /health（部署探针）
   └── 编排：检索 → 生成 → 格式化 → 存库
          │
          ├── retriever.py   本地 BM25 检索，不调模型
          ├── rag.py         调模型 + 引用白名单校验 + 安全降级
          └── env_utils.py   读 .env
          │
          ▼
       chat.db (SQLite)
          ├── messages   (id, session_id, role, content, created_at)
          └── api_usage  (day, used)
```

**既有资产**：358 项离线测试 · Railway 单 worker + 持久卷 · `DEPLOYMENT.md`

### 必须保留的能力

| # | 能力 | 为什么不能丢 |
| --- | --- | --- |
| 1 | **引用白名单校验** | 模型编造的来源永远不显示给用户 |
| 2 | **安全降级** | 坏 JSON / 异常 / 编造来源 → 统一降级，不崩 |
| 3 | **POST-Redirect-GET** | F5 不重复提问 |
| 4 | **单进程锁** | 防重复提交（也决定了**必须单 worker**） |
| 5 | **每日额度** | 保护演示费用 |
| 6 | **日志不记正文** | 隐私底线 |
| 7 | **不泄露异常原文** | 用户只看到固定提示 |
| 8 | **会话隔离** | 不同浏览器互不可见 |
| 9 | **rag.py 的公开契约** | 只返回三个键（见第 12 节） |

---

## 2. 四种 decision ⭐️ 本轮的核心里程碑

### 为什么必须改

原设计是「**检索为空 → 直接拒答，不调模型**」。
它省了钱，但**撑不住真实需求**：

| 用户会问 | 知识库里有吗 | 原设计的结果 |
| --- | --- | --- |
| 「帮我改这段作文」 | 部分 | ✅ 能答 |
| 「this 和 that 有什么区别」 | ❌ 没有 | ❌ **被拒答** —— 但这明明是正当的英语问题 |
| 「帮我把这句翻译成英文」 | ❌ 没有 | ❌ **被拒答** |
| 「虚拟语气怎么用」 | ❌ 没有（资料只讲现在完成时） | ❌ **被拒答** |

**如果只能回答知识库里那 3 份资料覆盖的内容，这个产品就只是个"资料问答器"，不是英语教练。**

### 四种 decision 的正式定义

| decision | 什么时候用 | citations | 调模型吗 | 占额度吗 |
| --- | --- | --- | --- | --- |
| **`answer`** | 检索片段**确实支持**回答 | ✅ **必须合法且非空** | ✅ | ✅ |
| **`general_answer`** | 是正常的英语学习 / 写作 / 翻译 / 词汇 / 语法问题，**但检索资料不支持回答** | ❌ **必须为空** | ✅ | ✅ |
| **`insufficient_evidence`** | 用户询问**知识库 / 课程事实**，资料只有部分依据 | ❌ 空 | ✅ | ✅ |
| **`refuse`** | 超出英语学习范围 / 危险请求 / 不能编造的业务事实 | ❌ 空 | 见下 | 见下 |

### `general_answer` 的页面表现 ⭐️

**它必须和 `answer` 在视觉上区分开** —— 因为它的依据是模型的通用知识，不是项目资料。

```
┌─────────────────────────────────────┐
│  AI 通用知识回答                     │   ← 明确的标识
│                                     │
│  this 和 that 的区别在于……           │
└─────────────────────────────────────┘
```

**绝不能**给它加「资料来源」—— 那等于伪造依据（见需求文档的伦理边界）。

### ⚠️ 路由规则：不再用「检索有没有结果」当开关

**原设计是「chunks 为空 → 只允许 `general_answer` 或 `refuse`」。这是错的。**

原因：**「检索到无关片段」和「检索不到片段」是两回事**，而原设计只处理了后者。

**一个会翻车的例子：**

> 用户问「this 和 that 有什么区别？」
> 检索器**捞到了**一段语法资料（词法上有重合），但那段资料**讲的是现在完成时，与 this/that 无关。**

| 原设计的结果 | 应该的结果 |
| --- | --- |
| chunks 非空 → 模型被卡在 `answer` 上，**必须引用那段无关资料** | **`general_answer`** —— 这是一个正当的英语问题，只是资料不支持 |

**所以规则改成：**

| 谁决定 | 什么时候 |
| --- | --- |
| **本地确定性规则** | 只用来**直接拒绝**那些显然不该回答的请求（见下） |
| **模型** | **其余每一次调用，都允许在四者中自由选择** |

> **一句话：模型每次都能选四个里的任何一个；只有极少数请求会在到模型之前就被本地挡掉。**

### 本地确定性规则只负责「明显不该答」（⚠️ 尚未实现）

> **先看清当前真实规则，再看计划。**
>
> 当前版本里**没有**下面这些本地拒绝规则，而且**检索为空也会调用模型**：
> 由模型去判 `general_answer`（正常的英语问题，用通用知识回答）还是 `refuse`
> （超出范围 / 不能编造的业务事实），并且**占用一次额度**。
> 理由是「检索不到」和「这不是个正当的英语问题」完全是两回事，只有模型能分辨。
>
> **只有空问题、超长问题、检索异常才不调模型** —— 也正因为没调模型，这三类才不占额度。
> （前两类在 `app.py` 里拦掉，检索异常在检索那一步就返回了。）
>
> 下面这张表是**计划**，落地之前不要按它理解现在的行为。

| 情况 | 处理 | 调模型吗 |
| --- | --- | --- |
| 空问题 / 超长问题 | 原有逻辑（**已实现**） | ❌ |
| 命中明确违规模式（考试作弊、生成恶意内容） | 本地固定话术拒绝（**计划，未实现**） | ❌ |
| 知识库已明确确认没有的**业务事实**（价格、排课、证书） | 本地固定话术拒绝（**计划，未实现**） | ❌ |

> 这三类落地后**不需要模型判断** —— 本地规则能确定地判出来，所以不花钱。
> **但在它们落地之前，别把「不花钱」当成现状说出去。**

### 每种 decision 的 citations 要求

| decision | citations |
| --- | --- |
| `answer` | **非空**，且每条都过白名单 |
| `general_answer` | **空** |
| `insufficient_evidence` | **空** |
| `refuse` | **空** |

> 只要 citations 不合规（`answer` 却没有、或其余三种却带了），
> **走原有的安全降级**，降为 `insufficient_evidence`。

### ⚠️ 每日额度怎么处理 —— 一个必须诚实说明的取舍

| 情况 | 占额度 |
| --- | --- |
| `answer` / `insufficient_evidence` | ✅ 占 |
| **`general_answer`** | ✅ **占** |
| 模型判断为 `refuse`（走了一次调用） | ✅ **仍然占** —— 已经发出过请求了 |
| 本地直接 `refuse`（没调模型） | ❌ 不占 —— **但这条路径还不存在**：本地拒绝规则尚未实现，现在每一次 `refuse` 都真实走过一次调用，因此都占额度 |

**代价说明：**

> **「检索不到资料」不再是省钱的路径了。**
>
> 以前它等于「不花钱地拒答」；现在**每一个有效问题都会走一次真实调用** ——
> 检索为空也不例外，哪怕模型最后判的是 `refuse`。
> （本地拒绝规则还没实现，所以没有「本地就能确定拒绝」这条路可走，见上文。）
>
> 这是**为了支持通用英语问题必须付的代价**。
> 换来的能力是：写作、翻译、词汇、任意语法问题都能被服务 ——
> **而这些恰好是用户访谈里最常提的需求。**

**防护措施：**

- **全站每日限额**（现有 `DAILY_API_LIMIT`）继续生效
- **新增每位学习者的每日限额**（见第 19 节安全要求）
- 明显不该答的走本地 `refuse`，不花钱（**计划中，尚未实现** —— 见前文「本地确定性规则」一节）

---

## 3. 最近对话上下文 ⭐️

### 为什么需要

用户访谈里出现了这类真实需求（见 `PERSONALIZATION_EVALS.md`）：

> 「**刚才那句话**再改一下」
> 「**再给一个例子**」
> 「**修改上面的段落**」

**只传当前这一句，这些都答不了。**

### 规则（五条硬性约束）

| # | 规则 | 说明 |
| --- | --- | --- |
| 1 | **只取当前会话的最近 6 条消息** | 不是全部历史，不是跨会话 |
| 2 | **有总字符上限** | 超过就从头截断，见下 |
| 3 | **只属于当前用户、当前会话** | 绝不能取到别人的 |
| 4 | **跨会话的长期信息走学习档案** | 不靠翻聊天记录 |
| 5 | **仍然只调用模型一次** | 上下文只是拼进那一次请求 |

### 字符上限怎么定

```
按时间从新到旧取消息，累计字符数，超过上限就停止
（即：优先保留最近的，最老的被丢掉）
```

**上限要保证两件事：**

- 加上检索结果和提示词之后，**总长仍在模型可接受范围内**
- **不会因为一条超长消息把预算吃光** —— 单条消息也要有上限

### 为什么是 6 条

| 太少了 | 太多了 |
| --- | --- |
| 3 条撑不住「刚才那句话」这类指代 | 20 条会挤占检索结果的位置，且成本上升 |

**6 条 ≈ 3 轮问答**，足够覆盖大多数指代场景，又不至于喧宾夺主。

> **这是一个可以被第一轮实测调整的参数**，设计上写死值、实现上留成常量即可。

### 与学习档案的分工

| | 最近上下文 | 学习档案 |
| --- | --- | --- |
| **管什么** | 这一轮对话里刚说过的东西 | 跨会话的长期信息 |
| **例子** | 「刚才那句话」「上面那段」 | 水平、偏好、生词、常犯错误 |
| **存哪** | `messages` 表（已有） | 新增的档案表 |
| **生命周期** | 跟着会话 | 跟着学习者 |

> **不要用聊天记录当长期记忆** —— 那是两套东西。
> 上下文解决「刚才说了什么」，档案解决「你是个什么样的学习者」。

---

## 4. 邀请码访问流程

```
① 测试组织者生成邀请码 → 手动发给某位测试用户
                    │
                    ▼
② 用户输入邀请码
                    │
                    ▼
③ 服务端算 HMAC-SHA256(key=INVITE_CODE_PEPPER, message=invite_code) → 得到摘要
                    │
                    ▼
④ 用摘要直接查 invites 表
        │
        ├── 查不到        → 「邀请码无效」
        ├── status=revoked → 「这个码已失效」
        ├── 已绑定 learner
        │      └── 绑定到该 learner，建立本次会话
        └── 未使用且有效
               └── 创建新 learner，把这张邀请码绑上去
                    │
                    ▼
⑤ 写入 learner_sessions（这个 session 属于这个 learner）
                    │
                    ▼
⑥ 之后所有请求：从 cookie 拿 session → 查 learner_sessions → 得到 learner
```

### 关键规则

| 规则 | 说明 |
| --- | --- |
| **未知邀请码不能自动创建用户** | 查不到就是查不到，绝不「先建个号再说」 |
| **只有「有效且未使用」的码才能创建 learner** | 状态机见第 5 节 |
| **一个 session 只能属于一个 learner** | `learner_sessions` 上建唯一约束 |
| **一个 learner 可以有多个 session** | 换设备、清 cookie 后重新输码 |
| **不在 URL 里带码** | 会被浏览器历史和代理日志记录 |
| **尝试必须限速** | 防暴力枚举 |

---

## 5. 邀请码摘要存储与安全要求

> ### ⚠️ 修正说明
>
> 原设计写的是「**每个码独立的盐**」+「**按哈希直接查询**」——
> **这两条是矛盾的**：加了随机盐之后，同一个码每次算出的哈希都不同，就没法直接查询了。
>
> **现在改为 HMAC + 服务端 pepper。**

### 正确方案：HMAC-SHA256 + 服务端 pepper

| 步骤 | 做法 |
| --- | --- |
| **① 生成** | 用**密码学安全**的随机源，生成**高熵**邀请码 |
| **② 摘要** | 计算 **`HMAC-SHA256(key = INVITE_CODE_PEPPER, message = invite_code)`** |
| **③ 存储** | 数据库**只保存摘要** |
| **④ 查询** | **用摘要直接查** —— 同一个码永远算出同一个摘要 |
| **⑤ 比对** | 用**安全比较函数**（常数时间），不用 `==` |

**pepper 在哪：**

- **只存在于环境变量**，和 `DEEPSEEK_API_KEY`、`FLASK_SECRET_KEY` 同级
- **绝不写进代码、绝不进数据库、绝不进日志**
- 换 pepper 会导致**所有已有邀请码失效** —— 这是一条运维上要知道的约束

### 为什么用 HMAC 而不是「随机盐 + 哈希」

| | 随机盐（原方案） | HMAC + pepper（现方案） |
| --- | --- | --- |
| 同一个码的摘要固定吗 | ❌ 每次不同 | ✅ 固定 |
| 能直接按摘要查询吗 | ❌ 不能 | ✅ 能 |
| 防彩虹表 | ✅ | ✅（pepper 就是全局密钥） |
| 防数据库泄露后离线爆破 | ✅ | ✅（没有 pepper 就算不出来） |

### 与普通口令哈希的区别

**这里不用 bcrypt/argon2 那类慢哈希** —— 理由：

- 邀请码是**高熵随机值**（不是人选的密码），**没有字典可猜**
- 需要**按摘要直接查询**，慢哈希会让每次请求都变慢
- 防爆破靠的是「**高熵 + pepper + 限速**」这三条，不是靠慢

### 邀请码生成要求

| 要求 | 说明 |
| --- | --- |
| **密码学安全随机** | 不用时间戳、不用自增、不用可预测的种子 |
| **足够长的高熵值** | 长到不可能被猜中或撞码 |
| **不含可识别信息** | 不包含用户编号、日期等能反推的信息 |
| **只展示一次** | 生成时给组织者看一次，之后系统里只有摘要 |

### 日志禁令

| ❌ 绝不进日志 |
| --- |
| 明文邀请码 |
| 邀请码摘要 |
| `INVITE_CODE_PEPPER` |

**理由**：拿到邀请码 = 访问那个用户的全部学习数据。**它和密钥是同一级别的凭证。**

### 邀请码生命周期

```
            生成
             │
             ▼
        ┌─────────┐
        │ active  │  未使用，可用来创建 learner
        └────┬────┘
             │ 用户首次使用
             ▼
        ┌─────────┐
        │ bound   │  已绑定某个 learner
        └────┬────┘
             │ 用户清空全部个人数据 / 组织者主动作废
             ▼
        ┌─────────┐
        │ revoked │  永久失效，不能再用
        └─────────┘
```

| 状态 | 含义 | 能否用来登录 |
| --- | --- | --- |
| `active` | 已生成、未使用 | ✅ 首次使用会创建 learner |
| `bound` | 已绑定 learner | ✅ 进入已有账号 |
| `revoked` | 已作废 | ❌ 永久无效 |

### ⚠️ 并发语义：两个请求可以都成功，但只能创建一个 learner

**真正的不变量是：**

> **全程只能创建一个 learner；所有成功建立的 session 都必须指向它。**
> **绝不能产生两个 learner。**

**注意 —— 这不是「一成一败」：**

两个并发请求**都可以成功登录**（各自建自己的 session），
但它们必须**连接到同一个 learner**。

### 事务顺序

```
BEGIN IMMEDIATE
    │
    ▼
查询邀请码（按 code_digest）
    │
    ├── status = 'active'
    │       │
    │       ▼
    │   ① 先创建 learner
    │       │
    │       ▼
    │   ② 条件更新：
    │      UPDATE invites SET status='bound', learner_id=?
    │       WHERE code_digest=? AND status='active'
    │       │
    │       ├── 影响行数 = 1  → 继续
    │       └── 影响行数 ≠ 1  → 【整体 ROLLBACK】并拒绝
    │
    └── status = 'bound'
            │
            ▼
        直接使用已有的 learner_id（【不创建新 learner】）
    │
    ▼
创建 learner_session（指向确定下来的那个 learner）
    │
    ▼
COMMIT
```

### 第二个并发事务会看到什么

```
事务 A：BEGIN IMMEDIATE → 读到 active → 创建 learner → 更新为 bound → COMMIT
                                                                     ↓
事务 B：BEGIN IMMEDIATE（【等待 A 提交】）→ 读到 bound → 直接用 A 创建的 learner_id
                                                                     ↓
                                             两个 session 指向【同一个 learner】 ✅
```

**三个关键点：**

| # | 点 |
| --- | --- |
| 1 | `BEGIN IMMEDIATE` **立刻取得写锁** —— 第二个事务必须**等第一个提交完**才能继续 |
| 2 | 它开始时邀请码**已经是 `bound`** → 走 `bound` 分支，**不会创建第二个 learner** |
| 3 | 即使时序意外，**「影响行数 ≠ 1 就整体回滚」**是最后一道防线 |

### 两条必须遵守的规则

| 规则 | 为什么 |
| --- | --- |
| **先创建 learner、再条件更新邀请码** | 顺序反过来的话，更新成功而创建 learner 失败时，会留下一个「已绑定但绑了个寂寞」的邀请码 |
| **影响行数不是 1 → 整体回滚** | UPDATE 改 0 行**不会报错**；不检查就会「以为成功了」 |

> ### 关于原子性的准确说法
>
> 靠的是 **`BEGIN IMMEDIATE` 事务 + 条件 UPDATE 语句本身的原子性**，
> **不是「数据库的行级写锁」** —— 那个说法不准确。
>
> SQLite 的 `BEGIN IMMEDIATE` 会立刻取得**写锁**，让并发的写事务**串行化**；
> 配合把判断条件写进 `WHERE` 的条件更新，
> 才能保证「**只有一个事务能把 `active` 改成 `bound`**」。

### 状态与 `learner_id` 的一致性约束

**两个字段不能各说各话。** 用 CHECK 把合法组合锁死：

| `status` | `learner_id` | 合法？ |
| --- | --- | --- |
| `active` | NULL | ✅ 还没人用 |
| `active` | 有值 | ❌ **矛盾** —— 都绑了 learner，怎么还是 active |
| `bound` | 有值 | ✅ 正常状态 |
| `bound` | NULL | ❌ **矛盾** —— 绑了谁？ |
| `revoked` | NULL 或有值 | ✅ 都合法（保留历史关联） |

```
CHECK (
  (status = 'active'  AND learner_id IS NULL) OR
  (status = 'bound'   AND learner_id IS NOT NULL) OR
  (status = 'revoked')
)
```

> **由数据库强制**，不靠应用层自觉 —— 应用层写错时数据库直接拒绝。

---

## 6. 匿名用户、会话和学习档案的关系

```
invite（邀请码）
   │  摘要唯一标识
   │  learner_id 可空（未使用时为空）
   ▼
learner（匿名学习者）
   │
   ├── learner_preferences   偏好
   ├── vocabulary_items      生词本
   ├── error_patterns        重复错误
   ├── practice_attempts     练习记录
   │
   └── learner_sessions ──→ session_id ──→ messages（聊天记录）
```

### 三条关键关系

| 关系 | 约束 |
| --- | --- |
| **一个 session 只能属于一个 learner** | `learner_sessions.session_id` **唯一** |
| **一个 learner 可以有多个 session** | 换设备 → 新增一行，不删旧的 |
| **一个 learner 可以有多张邀请码**（补发场景） | `invites.learner_id` 指向 learner |

> **为什么把邀请码和学习者拆开**：
> 补发邀请码时（旧的丢了），只需要**新增一条 invite 并指向同一个 learner**，
> 而不需要动 learner 本身，也不需要迁移数据。

---

## 7. 建议的数据表

> **全部新增，不改动现有的 `messages` 和 `api_usage`。**
> 所有子表都带 `FOREIGN KEY ... ON DELETE CASCADE`（见第 19 节）。

### `invites`

| 字段 | 类型 | 约束 |
| --- | --- | --- |
| `id` | INTEGER | PRIMARY KEY |
| `code_digest` | TEXT | **UNIQUE NOT NULL** —— HMAC 摘要 |
| `status` | TEXT | NOT NULL，**CHECK** 只能是 `active`/`bound`/`revoked` |
| `created_at` | TEXT | NOT NULL |
| `revoked_at` | TEXT | 可空 |
| `learner_id` | INTEGER | **可空**，FOREIGN KEY → `learners(id)` ON DELETE SET NULL |

### `learners`

| 字段 | 类型 | 约束 |
| --- | --- | --- |
| `id` | INTEGER | PRIMARY KEY |
| `created_at` | TEXT | NOT NULL |
| `last_seen_at` | TEXT | NOT NULL |
| `status` | TEXT | NOT NULL，**CHECK** 只能是 `active`/`deleted` |

> **注意：`learners` 里没有任何身份字段** —— 没有姓名、学校、邮箱。

### `learner_sessions`

| 字段 | 类型 | 约束 |
| --- | --- | --- |
| `id` | INTEGER | PRIMARY KEY |
| `learner_id` | INTEGER | NOT NULL，FOREIGN KEY → `learners(id)` ON DELETE CASCADE |
| `session_id` | TEXT | **UNIQUE NOT NULL** ← **一个 session 只能属于一个 learner** |
| `created_at` | TEXT | NOT NULL |
| `last_seen_at` | TEXT | NOT NULL |

### `learner_api_usage` ⭐️ 本轮新增

| 字段 | 类型 | 约束 |
| --- | --- | --- |
| `learner_id` | INTEGER | NOT NULL，FOREIGN KEY → `learners(id)` **ON DELETE CASCADE** |
| `day` | TEXT | NOT NULL —— UTC 日期 |
| `used` | INTEGER | NOT NULL DEFAULT 0 |
| —— | —— | **PRIMARY KEY (learner_id, day)** |

**作用**：每位学习者的**每日额度** —— 防止一个人把全站额度用完。

> ### ⚠️ 全站额度与个人额度必须在【同一个事务】里检查并占用
>
> 分两次事务做会出现两种坏情况：
>
> | 坏情况 | 怎么发生 |
> | --- | --- |
> | **部分扣额** | 个人额度扣成功了、全站额度扣失败 → 账对不上，额度被白吃 |
> | **并发超额** | 两个请求同时读到「还有名额」，然后各扣一次 → 两边都超 |
>
> **正确做法**：在**一个 `BEGIN IMMEDIATE` 事务**里，
> 依次检查并占用 `api_usage`（全站）和 `learner_api_usage`（个人）两张表；
> **任一个不够就整体回滚** —— 要么两个都扣，要么一个都不扣。
>
> 这条和现有的 `reserve_api_call()` 是同一个思路，只是多了一张表。

### `learner_preferences`

| 字段 | 类型 | 约束 |
| --- | --- | --- |
| `learner_id` | INTEGER | **PRIMARY KEY**，FOREIGN KEY → `learners(id)` ON DELETE CASCADE |
| `level_code` | TEXT | NOT NULL，**CHECK** 在白名单里 |
| `level_uncertain` | INTEGER | NOT NULL，CHECK 只能是 0 / 1 |
| `language_mode` | TEXT | NOT NULL，**CHECK** 在白名单里 |
| `length_mode` | TEXT | NOT NULL，**CHECK** 在白名单里 |
| `updated_at` | TEXT | NOT NULL |

### `vocabulary_items`

| 字段 | 类型 | 约束 |
| --- | --- | --- |
| `id` | INTEGER | PRIMARY KEY |
| `learner_id` | INTEGER | NOT NULL，FOREIGN KEY → `learners(id)` ON DELETE CASCADE |
| `word` | TEXT | NOT NULL |
| `added_by` | TEXT | NOT NULL，**CHECK** 只能是 `user`/`auto` |
| `status` | TEXT | NOT NULL，**CHECK** 只能是 `learning`/`mastered` |
| `correct_recalls` | INTEGER | NOT NULL DEFAULT 0 |
| `distinct_days` | INTEGER | NOT NULL DEFAULT 0 |
| `last_recalled_at` | TEXT | 可空 |
| `mastered_at` | TEXT | 可空 |
| `created_at` | TEXT | NOT NULL |
| —— | —— | **UNIQUE(learner_id, word)** —— 同一个人的同一个词不重复记 |

### `error_patterns`

| 字段 | 类型 | 约束 |
| --- | --- | --- |
| `id` | INTEGER | PRIMARY KEY |
| `learner_id` | INTEGER | NOT NULL，FOREIGN KEY → `learners(id)` ON DELETE CASCADE |
| `category` | TEXT | NOT NULL，**CHECK 必须在白名单类别里**（见第 19 节） |
| `evidence_count` | INTEGER | NOT NULL DEFAULT 1 |
| `first_seen_at` | TEXT | NOT NULL |
| `last_seen_at` | TEXT | NOT NULL |
| `status` | TEXT | NOT NULL，**CHECK 只能是 `candidate`/`active`/`resolved`** |
| —— | —— | **UNIQUE(learner_id, category)** —— 同一个错误类型累加，不重复建行 |

#### 三态生命周期 ⭐️

```
第一次出现
     │
     ▼
  candidate  ← 只是个观察，还不能下结论
     │
     │ 第二次出现（或用户主动确认）
     ▼
   active    ← 正式进入长期档案，可用于复习和个性化
     │
     │ 用户标记「我已经改掉了」/ 长期不再出现
     ▼
  resolved
```

| 状态 | 含义 | **常见错误列表** | **全部数据页** | 进个性化提示词 | 可用于出题 |
| --- | --- | --- | --- | --- | --- |
| `candidate` | 只出现过一次 | ❌ 不展示 | ✅ **以「待确认观察」显示，可删除** | ❌ **不进** | ❌ 不可以 |
| `active` | 确认是习惯性错误 | ✅ 展示 | ✅ 展示 | ✅ 进 | ✅ 可以 |
| `resolved` | 已改掉 | ✅ 展示（标记已解决） | ✅ 展示 | ❌ 不进 | ❌ 不可以 |

> ### ⚠️ 两个页面，两种展示 —— 这个区分很重要
>
> **`candidate` 不是「隐藏数据」，只是「不当作结论使用」。**
>
> | 页面 | 看到什么 |
> | --- | --- |
> | **常见错误列表**（面向学习） | ❌ 看不到 `candidate` —— 因为**还没确认它是你的习惯性错误** |
> | **「系统记得我什么 / 全部已保存数据」** | ✅ **看得到**，标注为「**待确认观察**」，并且**可以删除** |
>
> **为什么必须在全部数据页里显示：**
>
> 如果系统存了一条关于用户的数据，**却不让用户看到**，
> 那「你能查看自己的全部数据」这句话就是假的。
>
> **用户可以看不见结论，但不能看不见数据。**

> ### 为什么要三态，不只是「记 / 不记」
>
> **一次笔误不应该变成「你的常见错误」。**
>
> 但直接丢掉也不好 —— 万一下次又错了呢？所以**先存成 `candidate`**：
> 记下来，**不当作结论、不影响回答**，等第二次出现再升级。
>
> **例外**：用户主动说「这个我确实老错」→ **直接进 `active`**（用户说了算）。

### `practice_attempts` — 已删除冗余 `learner_id` ⭐️

| 字段 | 类型 | 约束 |
| --- | --- | --- |
| `id` | INTEGER | PRIMARY KEY |
| `vocabulary_item_id` | INTEGER | **可空**，FOREIGN KEY → `vocabulary_items(id)` ON DELETE CASCADE |
| `error_pattern_id` | INTEGER | **可空**，FOREIGN KEY → `error_patterns(id)` ON DELETE CASCADE |
| `prompt_kind` | TEXT | NOT NULL，**CHECK** 只能是 `sentence_making`/`correction` |
| `result` | TEXT | NOT NULL，**CHECK** 只能是 `correct`/`wrong` |
| `attempted_at` | TEXT | NOT NULL |
| —— | —— | **CHECK（两个外键恰好一个非空）** |

**所属 learner 从哪来**：通过 `vocabulary_item_id` 或 `error_pattern_id` **推导** ——
两张父表上都有 `learner_id`。

> ### ⚠️ 两处修正说明
>
> **修正一：原设计的 `item_type` + `item_id` 是坏的。**
>
> 那种「多态外键」在 SQLite 里**没法建立真正的外键约束** ——
> 数据库无法保证 `item_id` 指向的记录真的存在，删除时也不会级联。
> 改成两个可空外键 + CHECK 之后，引用完整性**由数据库保证**。
>
> **修正二：原设计在 `practice_attempts` 上还放了一个 `learner_id` —— 那是不一致漏洞。**
>
> 它带来一个**数据库没法阻止**的错误：
>
> > A 的 `learner_id` + B 的 `vocabulary_item_id` → **一条跨用户的练习记录**
>
> 写进去**不报错**，但会让「某个生词的练习历史」混进另一个人的档案。
>
> | 方案 | 做法 | 评价 |
> | --- | --- | --- |
> | **① 删掉冗余 `learner_id`** | learner 通过 item 推导 | ✅ **本项目采用** —— 从根上不可能不一致 |
> | ② 保留 + 复合外键 | `(learner_id, item_id)` 复合外键指向子表的唯一键 | 可行，但要改子表加唯一键，更复杂 |
>
> **冗余字段本身就是不一致的来源。删掉它，一致性由外键天然保证。**

---

## 8. 每张表保存什么、不保存什么

| 表 | ✅ 保存 | ❌ **不保存** |
| --- | --- | --- |
| `invites` | 摘要、状态、时间、绑定的 learner | **明文邀请码**、pepper |
| `learners` | 内部编号、时间、状态 | **任何身份字段**（姓名、学校、邮箱、IP） |
| `learner_sessions` | learner 与 session 的对应 | 用户代理、IP |
| `learner_preferences` | 选项代号、更新时间 | 用户的原话 |
| `vocabulary_items` | 单词、状态、跨时间回忆次数 | **在哪个句子里遇到的** |
| `error_patterns` | 错误**类型**、次数、时间 | **写错的原始句子** |
| `practice_attempts` | 对错、练习类型、时间 | **作答原文** |

### 核心原则

> **只存「复习所必需的」，不存「顺便能存的」。**
> 多存的每一条都是多一份泄露风险，而收益很小。

---

## 9. 哪些信息可以自动记录

| 信息 | 怎么来 | 需要用户同意吗 |
| --- | --- | --- |
| **错误类型计数** | 从每次修改自动分类，**但同一类要重复出现才进长期档案**（见第 19 节） | ❌ 不需要（可查看、可删） |
| **练习对错** | 用户作答后自动判定 | ❌ 不需要 |
| **最后活跃时间** | 自动 | ❌ 不需要 |
| **生词候选** | 系统在回答里标记「这个词你可能想记」 | ⚠️ **必须用户点一下才入库** |

---

## 10. 哪些信息必须由用户主动确认

| 信息 | 为什么必须问 |
| --- | --- |
| **水平** | 猜错会让所有回答偏难或偏简 —— 必须给描述性选项 + 允许「不确定」 |
| **语言偏好** | 有人要中英对照，有人要全英文 —— 猜不出来 |
| **长度偏好** | 同上 |
| **要记哪些生词** | 自动记住每个问过的词，会让生词本变成用户没同意过的大杂烩 |
| **所有删除操作** | 必须二次确认 |
| **接受邀请码的规则** | 发放时要说明「丢了找不回」 |

> **一条原则：能猜的不要问，猜不准的必须问。**

---

## 11. 一次问题的完整处理流程

```
用户提交问题
      │
      ▼
① 身份：cookie 里的 session → 查 learner_sessions → learner
      （公网版则走原有匿名流程，无档案）
      │
      ▼
② 读 learner_preferences（水平、语言、长度）
      │
      ▼
③ 空输入 / 超长输入检查（原有逻辑）
      │
      ▼
④ 单进程锁（防重复提交）
      │
      ▼
⑤ retriever.retrieve()   ← 本地检索，不花钱，【只按当前问题】
      │
      ▼
⑥ 取【当前会话】最近 6 条消息（有字符上限）
      │   ← 顺序就是「先检索、再取上下文」，和 app.py 一致；
      │     历史不参与检索，「这次能引用什么」不会跟着上一轮漂移
      │
      ▼
⑦ 占额度（全站 + 单用户两个限额都要检查）
      │   ← 当前版本：走到这里就一定会调用模型，
      │     所以【有效问题即便检索为空，也占一次额度】
      │
      ▼
⑧ rag.generate_answer_with_profile_signals(...)
      │   ← 唯一一次模型调用
      │   ← 输入：当前问题 + 检索结果 + 最近上下文 + 偏好
      │   ← 输出：公开三键结果 + profile_signals（内部）
      ▼
⑨ 分离：
      ├── 公开结果 → 走原有的引用校验 → 格式化 → 展示
      └── profile_signals → 【绝不进网页/日志】→ 只用于更新档案
      │
      ▼
⑩ 更新档案：
      ├── error_patterns 累加（白名单类别，重复才进长期档案）
      ├── 生词候选 → 只在用户确认后入库
      └── learner.last_seen_at
      │
      ▼
⑪ 存 messages → 写日志（不含正文）→ 302 跳转
```

> **⚠️ 上面这条是【当前主分支】，里面没有本地拒绝规则。**
>
> **未来计划（尚未实现）：** 在 ⑤ 与 ⑦ 之间加一条本地规则分支 ——
> 「检索为空 + 本地规则判定明显不该答」直接本地 `refuse`，不调模型、不占额度。
> 落地之前，别按它理解现在的行为。
>
> **当前真实规则：有效问题即使检索为空，也会调用模型**（由它判 `general_answer` / `refuse`），
> **并占一次额度**。只有空问题 / 超长问题 / 检索异常才不调模型 —— 也正因为没调模型，这三类才不占额度。

---

## 12. 如何确保每题最多调用一次模型

### 三条保证

| # | 保证 | 怎么做到 |
| --- | --- | --- |
| 1 | **一次提问 = 一次模型调用** | 检索、上下文、偏好**全部拼进同一次请求** —— 不为它们单独调用 |
| 2 | **档案更新不额外调模型** | 用内部入口 `generate_answer_with_profile_signals()`，**同一次返回**里带上信号 |
| 3 | **额度在调用前占，失败也算** | 沿用现有逻辑 |

### 关键：`rag.py` 的改动范围 ⭐️

| 项 | 变不变 |
| --- | --- |
| **公开返回值的形状** | ✅ **不变** —— 仍然只有 `decision` / `answer` / `citations` 三个键 |
| **`decision` 的合法值** | ⚠️ **从 3 个增加到 4 个**（多了 `general_answer`） |
| **新增内部入口** | ✅ `generate_answer_with_profile_signals()` |

| 入口 | 返回 | 谁用 |
| --- | --- | --- |
| **`generate_answer()`** | 只有三个键 | 网页、评测器、任何外部调用 |
| **`generate_answer_with_profile_signals()`** | `(三键结果, profile_signals)` 二元组 | **仅个性化功能内部** |

> ### ⚠️ 这不是「纯加法」—— 已有测试必须改
>
> **原稿写的「现有 67 项 `test_rag` 测试全部继续有效」是错的，已删除。**
>
> 因为 `decision` 的合法值从 3 个变成 4 个，**下面这些地方都必须更新：**

| 需要更新的地方 | 改什么 |
| --- | --- |
| **`rag.py` 的 `VALID_DECISIONS`** | 从 3 个值改为 **4 个** |
| **`rag.py` 的提示词** | 加入 `general_answer` 的定义和引用规则 |
| **`rag.py` 的校验逻辑** | `answer` 必须非空引用；**其余三种必须空引用** |
| **`test_rag` 的相关测试** | 见下表 |
| **评测器 `evals/run_rag_eval.py`** | 判分要认识第四个值 |
| **验收题库 `evals/rag_cases.json`** | 可能需要新增期望 `general_answer` 的题 |

**具体哪些旧测试必须改写：**

| 旧测试 | 为什么必须改 |
| --- | --- |
| **「chunks 为空时不调用模型」** | ❌ **这条已经不成立了，而且已经改完了** —— 现在检索为空【仍然调用模型】（由它判 `general_answer` / `refuse`），并占一次额度。本地拒绝规则尚未实现，所以「命中本地规则才不调模型」目前无处可改 |
| **「chunks 为空 → `refuse`」** | ❌ 同上，现在可能返回 `general_answer` |
| **任何断言 `decision` 只有 3 个合法值的** | ⚠️ 要加上第四个 |

> **把这些列清楚，是为了避免编码阶段「以为只是加了个功能」** ——
> 实际上它**改变了已有模块的契约**，必须连带更新测试。

**`profile_signals` 的位置仍然是安全的**：

- 它是元组的**第二个元素**，取公开结果的那段代码**根本拿不到它**
- 如果个性化以后要下线，**只要不调那个内部入口就行** ——
  但注意：**`decision` 多出来的第四个值不会因为下线而消失**，那是产品能力，不是个性化功能

### `profile_signals` 的内容与处理

| 字段 | 内容 |
| --- | --- |
| `error_patterns` | 本次涉及的错误类型（**必须在白名单类别里**） |
| `vocabulary_candidates` | 候选生词（**只做提示，不自动入库**） |

**处理规则：**

| 规则 | 说明 |
| --- | --- |
| **绝不进网页** | 网页只拿公开三键结果 |
| **绝不进日志** | 日志只记「本次记录了 N 条」 |
| **非法信号直接丢弃** | 不在白名单里的类别、格式不对的字段 → **当次没有信号**，不影响公开回答 |

> **个性化信息的缺失，绝不能让主流程失败。** 这是现有安全降级思路的延续。

---

## 13. 查看、删除单条、清空档案、清空全部数据

| 操作 | 删什么 | **不删什么** |
| --- | --- | --- |
| **删除单条** | 那一条（如某个生词） | 其余记录 |
| **清空学习档案** | 偏好、生词、错误、练习记录 | **聊天记录**、learner 本身 |
| **清空全部个人数据** | 见下方完整流程 | —— |

### 「清空全部个人数据」的完整流程 ⭐️ 已修正

**必须在【同一个数据库事务】里完成，要么全成功，要么全不动：**

```
① 找到该 learner 的所有 session（查 learner_sessions）
      │
      ▼
② 删除这些 session 对应的【全部 messages】
      │
      ▼
③ 删除 learner_preferences
      │
      ▼
④ 删除 vocabulary_items（practice_attempts 会被级联删除）
      │
      ▼
⑤ 删除 error_patterns（practice_attempts 会被级联删除）
      │
      ▼
⑥ 删除 practice_attempts（兜底，防止漏网）
      │
      ▼
⑦ 删除 learner_sessions
      │
      ▼
⑧ 删除 learner
      │
      ▼
⑨ 【作废对应的邀请码】—— status = revoked
      │
      ▼
⑩ 提交事务
```

### 三条必须遵守的规则

| # | 规则 |
| --- | --- |
| 1 | **整个操作在同一个事务里** —— 中途失败就全部回滚，不能留下半截 |
| 2 | **原邀请码之后必须无效** —— 作废后不能再用来登录 |
| 3 | **重新参加需要新邀请码** —— 旧的永远不会被"复活" |

> ### ⚠️ 修正说明
>
> 原设计写的是「**清空后用同一个邀请码访问，应当回到全新用户状态**」——
> **这和「作废邀请码」是矛盾的**：码都作废了，怎么可能还用它回来？
>
> **现在统一为：清空 = 作废该邀请码。**
> 用户要重新参加，**必须由组织者发一张新的邀请码**。
>
> 这样更干净：**删除就是删除，不留一条能重新进入的通道。**

### 「清空学习档案」的不同之处

它**不清 learner、不清邀请码、不清聊天记录** ——
只是把学习偏好和记录清掉。所以用户**还能用同一个码继续用**，只是从零开始积累。

---

## 14. Cookie 丢失、邀请码丢失、换设备

| 情况 | 用户体验 | 数据会怎样 | 怎么恢复 |
| --- | --- | --- | --- |
| **清掉浏览器 cookie** | 回到「请输入邀请码」 | 数据**都在** | 重新输码 → 恢复 |
| **换一台设备** | 同上 | 数据**都在** | 重新输码 → 恢复 |
| **邀请码也丢了** | —— | ⚠️ **找不回来** | **无法自助恢复** |
| **清空了全部数据** | 码失效 | **全部删除** | **必须用新码重新参加** |

### 邀请码丢失是这套方案最大的代价

**必须在发放时就说清楚：**

> 「这个码是你唯一的凭证，请保存好。
> 丢了之后**我们也没法帮你找回学习记录** ——
> 因为系统里只存了它的摘要，**我们自己也不知道你的码是什么**。」

**补救方式：**

- 组织者**作废旧码 → 发一张新码 → 绑定到同一个 learner**
  （这正是把邀请码和学习者拆开的价值）
- 但**如果用户已经清空了全部数据，就没有 learner 可绑了** —— 只能全新开始

---

## 15. 不同用户之间的数据隔离

### 两层隔离

| 层 | 依据 | 管什么 |
| --- | --- | --- |
| **会话层** | `session_id` | 聊天记录的展示与上下文 |
| **学习者层** | `learner_id` | 学习档案 |

### 硬性要求

| # | 要求 |
| --- | --- |
| 1 | 所有档案查询**必须带 `learner_id`** |
| 2 | 所有聊天查询**必须带 `session_id`**（现有规则） |
| 3 | **最近上下文只取当前 session 的消息**（不能跨 session 拼） |
| 4 | 模型**绝不能**看到其他用户的数据 |
| 5 | 被问「别人在学什么」时**明确拒绝** |

### ⚠️ 两个最容易出错的地方

1. **把档案拼进提示词时** —— 拼错就会把别人的数据答出来，**用户能直接看到**
2. **取最近上下文时** —— 如果按 `learner_id` 取而不是按 `session_id`，会把用户**另一个设备上的对话**拼进来

> 这两处**必须单独写测试**（见第 22 节）。

---

## 16. 日志中禁止出现的内容

**在现有字段白名单的基础上的新增禁令：**

| ❌ 不能出现在日志里 | 为什么 |
| --- | --- |
| 用户问题正文 / AI 回答正文 | 已有规则 |
| **明文邀请码** | **和密钥同级** —— 拿到它就能访问那个用户的数据 |
| **邀请码摘要** | 同样是凭证 |
| **`INVITE_CODE_PEPPER`** | 密钥 |
| 原始 session_id | 已有规则 |
| **生词内容 / 错误类型明细** | 用户的学习数据（可记「N 条」，不记具体是什么） |
| **`profile_signals` 内容** | 同上 |
| API 密钥 / `FLASK_SECRET_KEY` | 已有规则 |
| 异常原文 | 已有规则（只记类型） |

**可以记**：learner 内部编号、事件名、计数、耗时。

> **原则不变：记「发生了什么」，不记「内容是什么」。**

---

## 17. 当前公网版本与个性化测试版本的隔离

> **这是一条硬门禁，不是建议。**

| | 当前公网版（Railway） | 个性化测试版 |
| --- | --- | --- |
| 访问 | 任何人 | **必须输入有效邀请码** |
| 功能 | 现有通用问答 | 通用问答 + 个性化档案 |
| 数据 | 现有 `messages` | **独立的数据库文件** |

### 怎么保证不越界

| # | 措施 |
| --- | --- |
| 1 | 个性化代码**在访问控制完成前不部署到公网版** |
| 2 | **两者的 `CHAT_DB_PATH` 必须指向不同文件** |
| 3 | 上线前手动验证四条（见下） |

### 上线前必须手动执行的验证

| # | 验证 | 期望 |
| --- | --- | --- |
| 1 | 不带邀请码访问档案页 | **被拒绝** ← **不通过就不能上线** |
| 2 | 用无效邀请码 | 被拒绝 |
| 3 | 用有效邀请码 | **只能看到自己的数据** |
| 4 | 公网版原有问答 | 照常工作 |

---

## 18. 数据库完整性约束

### 全局要求

| # | 要求 |
| --- | --- |
| 1 | **所有外键都写 `ON DELETE CASCADE`**（`invites.learner_id` 除外，用 `SET NULL`） |
| 2 | **每个连接都要执行 `PRAGMA foreign_keys = ON`** |
| 3 | 状态类字段加 **CHECK** 约束 |
| 4 | 需要唯一的组合加 **UNIQUE** 约束 |

> ### ⚠️ SQLite 的一个坑
>
> **SQLite 默认是【关闭】外键的** —— 不显式打开的话，写了 `FOREIGN KEY` 也不会生效，
> 删除父记录时子记录会变成孤儿数据，而且**不报错**。
>
> 而且这个 PRAGMA 是**连接级**的，不是数据库级的 ——
> **每开一个新连接都要设一次**，不能只在建表时设。

### 约束一览

| 表 | CHECK | UNIQUE |
| --- | --- | --- |
| `invites` | `status IN ('active','bound','revoked')` | `code_digest` |
| `learners` | `status IN ('active','deleted')` | —— |
| `learner_sessions` | —— | **`session_id`** ← 一个 session 只属于一个 learner |
| `learner_preferences` | 三个 mode 各自的白名单 | —— |
| `vocabulary_items` | `added_by`、`status` 白名单 | **`(learner_id, word)`** |
| `error_patterns` | `category` 白名单、`status` 白名单 | **`(learner_id, category)`** |
| `practice_attempts` | `prompt_kind`、`result` 白名单<br>**两个外键恰好一个非空** | —— |

---

## 19. 安全要求

### ① CSRF 保护 ⭐️

**所有会修改数据的操作都必须有 CSRF 保护**，尤其是：

| 操作 | 必须保护 |
| --- | --- |
| 修改偏好 | ✅ |
| 删除单条记录 | ✅ |
| 清空学习档案 | ✅ |
| **清空全部个人数据** | ✅ |
| 输入邀请码 | ✅ |

**要求：**

- 每个表单带一个**一次性/随会话的 token**
- 服务端校验，**不通过直接拒绝**（不执行任何删除）
- 现有应用的 cookie 已经有签名机制，可以在此基础上做

> **为什么这条重要**：删除是不可逆的。如果只有 cookie 鉴权而没有 CSRF 保护，
> 一个恶意页面就能诱导用户的所有浏览器去提交删除请求。

### ② 错误类型白名单 ⭐️

**模型不能自由发明错误类型。**

| | |
| --- | --- |
| **做法** | 在代码里维护一份**固定的错误类别白名单** |
| **模型返回的类别不在白名单里** | **直接丢弃**，不写入档案 |
| **好处** | ① 防止模型编出奇怪的类别把档案搞乱<br>② 让复习功能可以针对每个类别设计固定题型 |

**白名单示例（第一版）：**

`third_person_s` · `tense` · `spelling` · `article` · `preposition` · `plural` · `word_choice` · `sentence_structure`

> 这份清单**由代码维护，不由模型决定** —— 加新类别是一次代码改动，不是模型自由发挥。

### ③ 错误进长期档案的门槛 ⭐️

**不是出现一次就记。**

| 规则 | 说明 |
| --- | --- |
| **默认** | 同一类别**至少重复出现 2 次**才进入长期档案 |
| **例外** | **用户主动确认**「这个我确实老错」→ 立刻进入 |
| **为什么** | 一次性的笔误不应该变成"你的常见错误" —— 那会让档案失真 |

> 这直接对应需求文档里的「记录**重复**错误」—— **重点是「重复」两个字**。

### ④ 删除页面的说明要准确 ⭐️

删除确认页必须写明：

> - ✅ 这会删除**本系统保存的**你的数据
> - ⚠️ **不会**影响第三方服务商可能保留的数据

**为什么必须写**：回答生成要把内容发给模型服务商，
**我们删掉本地数据，控制不了对方怎么处理**。
含糊地说「彻底删除」是不诚实的。

### ⑤ 双重额度限制 ⭐️

| 层级 | 作用 |
| --- | --- |
| **全站每日限额**（现有 `DAILY_API_LIMIT`） | 保护演示总费用 |
| **每位学习者每日限额**（新增） | 防止**一个人把全站额度用完** |

**两者同时生效，取先到的那个。**

**为什么需要单用户限额**：访谈里已经有用户问「如何一天掌握语法大全」——
这种用户会连续问很多次。**没有单用户限额的话，一个人就能让全站当天停摆。**

---

## 20. 数据库迁移和回滚

| 原则 | 说明 |
| --- | --- |
| **只加不改** | 新表用 `CREATE TABLE IF NOT EXISTS`；**不动 `messages` 和 `api_usage`** |
| **启动时自动建表** | 沿用现有 `init_db()` |
| **不需要数据迁移** | 新表是空的 |
| **向后兼容** | 新代码能跑在旧库上（缺表就补建） |

### 回滚

| 情况 | 怎么做 | 数据会怎样 |
| --- | --- | --- |
| **代码回滚** | Railway Redeploy 上一个版本 | 新表还在，旧代码不认识但不影响 |
| **要清掉个性化数据** | 手动删那几张表 | 聊天记录不受影响 |
| **数据库整个坏掉** | 用卷备份恢复 | 见 `DEPLOYMENT.md` 第九节 |

> ⚠️ **回滚代码不会回滚数据** —— `DEPLOYMENT.md` 已写明。

### 一条运维约束

**换 `INVITE_CODE_PEPPER` 会让所有已有邀请码失效** ——
因为摘要变了，查不到。换之前要知道这一点。

---

## 21. 最小文件改动范围

### 需要改的现有文件

| 文件 | 改什么 | 风险 |
| --- | --- | --- |
| `app.py` | ① 邀请码校验入口 ② 取最近上下文 ③ 档案路由 ④ 拆分公开结果与 `profile_signals` ⑤ CSRF | **中高** —— 主流程要动 |
| **`rag.py`** | ① **新增 `general_answer`** ② **修改 `chunks` 为空时的行为** ③ 更新提示词与 citations 校验 ④ 新增内部入口 `generate_answer_with_profile_signals()` | **中等风险** —— **公开结构不变，但已有行为和 `decision` 枚举发生变化** |
| **`test_rag.py`** | 改写受影响的旧测试 | **中等** —— 清单见第 12 节 |
| **`evals/run_rag_eval.py`** | 判分要认识 `general_answer`（第四个合法值） | **中等** —— 改动小，但直接影响判分 |
| **`evals/rag_cases.json`** | **按验收需要**增加或调整 `general_answer` 的题 | 低 |
| `templates/index.html` | 加「我的档案」入口；`general_answer` 的标识 | 低 |

### `rag.py` 的边界（重要）

| | |
| --- | --- |
| ✅ **可以改** | `generate_answer()` 的**内部实现** |
| ✅ **可以加** | `general_answer` 这个新的合法值 |
| ✅ **可以改** | **`chunks` 为空时的行为** |
| ✅ **必须加** | 内部入口 `generate_answer_with_profile_signals()` |
| ❌ **不能改** | **函数签名** |
| ❌ **不能改** | **公开返回结构** —— 仍然只有 `decision` / `answer` / `citations` 三个键 |

> ### ⚠️ 「公开结构不变」≠「行为不变」
>
> **结构不变**是为了让调用方不用改代码；**但行为确实会变** ——
>
> 1. `decision` 多了一个合法值（`general_answer`）
> 2. `chunks` 为空时也可能调用模型
> 3. 提示词和 citations 校验都要改
>
> **所以 `rag.py` 不是「纯加法」，而是「兼容性改动」** ——
> 外部调用方不用改，但**已有的测试和评测器必须跟着更新**。

### 新增的文件

| 文件 | 干什么 |
| --- | --- |
| `profile_store.py` | **档案的全部读写** —— 邀请码、learner、偏好、生词、错误、练习 |
| `templates/invite.html` | 邀请码输入页 |
| `templates/profile.html` | 档案页（查看 / 删除 / 清空） |
| `test_profile_store.py` | 档案层的离线测试 |

### 明确不动的

- **`retriever.py`**
- **`env_utils.py`**
- **`messages` 表结构**
- **`api_usage` 表结构**

> ⚠️ **注意 `evals/` 的范围** —— 上面只列了**两个具体文件**要改
> （`run_rag_eval.py`、`rag_cases.json`），**不是整个 `evals/` 目录**。
> `evals/` 下其余文件不动。
>
> 原稿写的「`evals/` 明确不动」是**错的** —— 那是笼统地把整个目录划出去，
> 而实际上评测器的判分逻辑**必须**认识第四个 decision 值。

---

## 22. 可以自动化测试的验收点

| # | 验收点 | 怎么测 |
| --- | --- | --- |
| 1 | 无邀请码 → 被拒绝 | 直接请求 |
| 2 | **未知邀请码不能创建用户** ⭐️ | 用一个不存在的码 → 查 `learners` 表**没有新增** |
| 3 | 有效未使用的码 → 创建 learner 并绑定 | 查库 |
| 4 | 已绑定的码 → 进入已有 learner | 查库 |
| 5 | **已作废的码 → 被拒绝** | 查库 + 请求 |
| 6 | **A 的档案不出现在 B 的请求里** ⭐️ | 两个 learner 交叉请求 |
| 7 | **A 的历史不出现在 B 的上下文里** ⭐️ | 同上，检查提示词 |
| 8 | 档案内容正确拼进提示词 | 检查假客户端收到的 messages |
| 9 | **最近上下文只取当前 session** ⭐️ | 造两个 session，检查提示词 |
| 10 | **上下文有字符上限** | 塞超长历史，检查被截断 |
| 11 | 偏好改了立刻生效 | 改偏好 → 再问 |
| 12 | 偏好跨会话保留 | 重载模块 |
| 13 | **每题只调用一次模型** ⭐️ | 断言 `len(fake.calls) == 1` |
| 14 | **普通英语问题 → `general_answer`** ⭐️ | 用知识库没有的问题 |
| 15 | **`general_answer` 无引用** | 检查 citations 为空 |
| 16 | 无资料翻译请求仍能回答 | 翻译请求 |
| 17 | **`profile_signals` 不泄露** ⭐️ | 检查网页响应和日志里没有它 |
| 18 | 非法信号被丢弃且不影响公开回答 | 喂坏数据 |
| 19 | **CSRF 缺失 / 错误 → 拒绝删除** ⭐️ | 不带 token 提交 |
| 20 | 删除单条后不再被引用 | 删 → 追问 |
| 21 | 清空档案后回答回到默认风格 | 清 → 再问 |
| 22 | **全部清空后旧邀请码失效** ⭐️ | 清 → 用旧码登录 → 被拒绝 |
| 23 | **全部清空是原子的** | 中途造错，检查没有半截状态 |
| 24 | **外键级联生效** ⭐️ | 删 learner → 子表全空 |
| 25 | **`PRAGMA foreign_keys` 确实打开** | 直接查 |
| 26 | 日志里没有邀请码、摘要、生词、正文 | assertLogs |
| 27 | 邀请码不出现在 URL 或页面里 | 检查响应 |
| 28 | 邀请码尝试被限速 | 连续请求 |
| 29 | **单用户限额与全站限额同时生效** ⭐️ | 分别打满 |
| 30 | 无检索结果 → **仍然调用模型**（判 `general_answer` / `refuse`）并占一次额度；只有空问题 / 超长 / 检索异常才不调模型也不占额度 | 已由现有测试覆盖（`test_empty_retrieval_still_calls_the_model`、`test_empty_retrieval_still_consumes_quota`） |
| 31 | **检索到无关片段时仍能进入 `general_answer`** ⭐️ | 构造「捞到但无关」的检索结果 |
| 32 | **两个并发请求用同一邀请码：都成功，但只创建一个 learner** ⭐️ | 并发发起两次首次绑定，断言 `learners` **只多一行** + 两个 session 的 `learner_id` **相同** |
| 33 | **绑定检查影响行数为 1** | 构造 status 已被改掉的场景，确认被拒绝 |
| 34 | **`candidate`：常见错误列表看不到、不进提示词、但全部数据页看得到** ⭐️ | 只犯一次错 → 查三处 + 验证**可删除** |
| 35 | 同类错误第二次才升 `active` | 连犯两次 |
| 36 | **全站与个人额度在同一事务** ⭐️ | 构造「个人够、全站不够」，确认**两个都没扣** |
| 37 | **现有 358 项测试为基线** ⭐️ | 跑全套，**但需先按下节说明更新受影响的旧测试** |

> ### ⚠️ 注意：第 37 条不是「原样通过」
>
> `decision` 从 3 个值变成 4 个，**已有测试里有一部分必须改写**（见第 12 节）。
> 编码阶段**先改测试、再跑全套** —— 否则会把「测试没更新」误判成「代码写错了」。

---

## 23. 仍需人工判断的验收点

| # | 验收点 | 为什么必须人工 |
| --- | --- | --- |
| 1 | **回答的自然度**（K04） | 主观判断，需 3 位评审者 1–5 分 |
| 2 | **「读起来仍然像我自己写的」** | 只有用户本人能判断 |
| 3 | **原意有没有被改变** | 需要理解语义 |
| 4 | **默认修改是不是「温和」** | 程度判断 |
| 5 | **不同水平下回答难度的差异是否明显** | 需要人来感受 |
| 6 | **`general_answer` 的标识是否清楚** | 用户能不能看懂「这是通用知识」 |
| 7 | **「不装瞎」的度**（J15） | 该提示什么、不该直接改什么 |
| 8 | **档案页好不好用** | 可用性需要真实用户 |
| 9 | **邀请码的说明用户看懂了吗** | 需要真实用户 |

---

## 24. 已知限制和风险

### 限制

| # | 限制 | 说明 |
| --- | --- | --- |
| 1 | **邀请码丢了找不回** | 摘要不可逆 |
| 2 | **清空全部数据后必须用新码** | 删除就是删除，不留后门 |
| 3 | **没有登录、没有密码** | 邀请码就是全部凭证 |
| 4 | **必须单 worker** | 现有锁和 SQLite 决定 |
| 5 | **通用问题也占额度** | 支持 `general_answer` 的代价 |
| 6 | **上下文只有 6 条** | 更早的指代仍然答不了 |
| 7 | **错误分类依赖模型** | 可能误判（靠白名单 + 2 次门槛缓解） |
| 8 | **换 pepper 会让所有码失效** | 运维约束 |

### 风险

| # | 风险 | 严重度 | 缓解 |
| --- | --- | --- | --- |
| 1 | **档案或上下文拼错 → 泄露他人数据** | 🔴 高 | 单独测试（第 22 节 #6、#7、#9） |
| 2 | **邀请码或其摘要进入日志** | 🔴 高 | 字段白名单 + assertLogs |
| 3 | **个性化功能误上线到公网版** | 🔴 高 | 部署门禁 + 上线前手动验证四条 |
| 4 | **删除操作没有 CSRF 保护** | 🔴 高 | 所有写操作加 token + 测试 |
| 5 | **清空操作不是原子的** | 🟡 中 | 单事务 + 中途造错的测试 |
| 6 | **外键没打开 → 孤儿数据** | 🟡 中 | 每连接设 PRAGMA + 级联测试 |
| 7 | **单用户刷爆全站额度** | 🟡 中 | 单用户限额 |
| 8 | **提示词变长导致回答质量下降** | 🟡 中 | 第一轮对比有无个性化的质量 |
| 9 | **档案越记越多、变得没用** | 🟡 中 | 生词需确认；错误类型要重复 2 次 |
| 10 | **「保留用户声音」假设不成立** | 🟡 中 | **这正是第一轮要验证的问题** |

### 最后一条不是风险，是要回答的问题

「保留用户声音」从一开始就是**假设**（见 `PRODUCT_DISCOVERY.md`）。

- **成立** → 三种修改行为（按需触发）值得继续投入
- **不成立** → 说明用户抱怨的「AI 味」是别的东西，**需要重新理解那个痛点**

> **把假设验证失败当成一个正常结果。**
> 这正是第一轮要用**邀请制小范围测试**而不是直接上线的根本原因。
