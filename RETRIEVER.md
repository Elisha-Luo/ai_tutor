# 本地检索层（RAG 第一步）

这个文档解释 `retriever.py` 在做什么、为什么这么做、以及**它现在能证明什么、还不能证明什么**。

---

## 一、什么是「切分」（chunking）

### 先看个问题

知识库里三份资料，加起来大约一万字。用户问「since 和 for 有什么区别？」

最直接的做法是：把这一万字**全部**塞给模型，再附上问题。

但这有三个麻烦：

1. **贵。** 每次提问都要按一万字计费，问十次就是十万字。
2. **慢。** 模型要读完一万字才开始回答。
3. **不准。** 相关内容只有一小段，其余全是干扰。资料越长，模型越容易抓错重点，或者把别处的说法混进来。

### 解决思路

先**把资料切成小段**，提问时只挑出最相关的那几段送过去。

```
一整份资料（约 3000 字）
        ↓  切分
┌────────┬────────┬────────┬────────┐
│ 片段 1 │ 片段 2 │ 片段 3 │ 片段 4 │
└────────┴────────┴────────┴────────┘
        ↓  只挑最相关的 3 段
   问题 + 片段 2 + 片段 5 + 片段 7  →  交给模型
```

这就是**切分**：把长文档拆成一个个语义完整的小块。

### 怎么切才合理

切法有很多种，常见的有：

| 切法 | 说明 |
| --- | --- |
| 按固定字数切 | 简单，但会把一句话、一个意思从中间切断 |
| 按段落切 | 好一些，但段落长短不一 |
| **按标题层级切** | **本项目采用**——一篇文档的结构本来就是作者按主题分好的，顺着它切，每块天然是一个完整主题 |

本项目按 **Markdown 的二级标题（`##`）** 切。原因是这三份资料的作者（也就是这个项目自己）本来就是按主题分的节：`## since 和 for 的区别` 这一节讲的就是 since 和 for，不会有别的。

`###` 三级标题**不**作为切分点，它下面的内容会留在所属的二级标题块里。比如「四种常见用法」这一节下面有四个 `###` 小标题，它们合成一段——因为这四者合起来才是「现在完成时有哪些用法」这个完整主题。

### 每段必须记住自己从哪来

切完之后，每一段都要牢牢记住自己的出处：

```python
{
    "source":     "grammar_present_perfect.md",   # 出自哪个文件
    "heading":    "since 和 for 的区别",            # 出自哪个标题
    "text":       "这是最容易出错的地方……",         # 正文
    "doc_title":  "现在完成时（Present Perfect）"    # 整份文档的标题
}
```

**这一点不能省。** 没有出处，模型答得对不对就没法查证，用户也没法自己去核对。RAG 相比普通对话最大的价值就是「有据可查」，而这个「据」就是靠这几个字段撑起来的。

---

## 二、还有一件必须做的事：排除说明文档

`knowledge_base/README.md` 是**语料库的说明文档**，不是教学内容。它讲的是「这份语料是模拟的、可以公开、怎么替换」。

如果把它也索引进去，会出两个问题：

1. 用户问「RAG 是什么」「这个知识库能公开吗」，可能检索到它，然后被当作**教学依据**引用出来——驴唇不对马嘴。
2. 它和内容文件放在同一个目录，不主动排除的话，任何「读取整个目录」的代码都会顺手把它读进去。

所以 `retriever.py` 里有一份明确的排除名单：

```python
EXCLUDED_FILES = {"readme.md"}
```

并且有一条专门的测试盯着它：**索引里的来源只能有三份，永远不能出现 README.md。**

> 更彻底的做法是把说明文档移出 `knowledge_base/`。目前先用排除名单，改动最小。

---

## 三、检索是怎么打分的

### 一句话版本

把问题和每一段都切成词，看哪些词重合。重合得多、而且那些词越**罕见**，分数就越高。

### 具体一点

**第一步：切词。** 英文有空格，直接按空格切。中文没有空格，又不想引入分词库（那会多一个依赖），所以用最经典的无依赖办法——**相邻两个字切成一片**：

```
"现在完成时"  →  ["现在", "在完", "完成", "成时"]
```

为什么用两个字而不是一个字？单个汉字太常见了（「的」「是」「了」几乎每段都有），区分不了任何东西。「完成」这种两个字组合才有意义。

**第二步：算分。** 用的是 **BM25**——信息检索领域用了很多年的经典排序公式。它的核心思想有两条：

- **一个词越罕见，越说明问题。** 「的」到处都有，命中了不算什么；「since」只在这份资料的少数几段里有，命中了就很有价值。这叫 IDF。
- **同一个词出现很多次，分数不会无限涨。** 一段里出现 10 次「复习」，不代表它比出现 5 次的强一倍。涨到一定程度就饱和。这叫词频饱和。

**第三步：给标题加权。** 标题里的词重复算 3 遍，整份文档的标题算 2 遍。因为标题是强信号——一节叫「since 和 for 的区别」，那它十有八九就是讲这个的。

**第四步：限制单个文件最多贡献几段。** 见下面「取舍」一节。

---

## 四、方案取舍：为什么是词法检索

### 这是词法检索，不是向量检索

| | 词法检索（本项目的现状） | 向量检索（以后要做） |
| --- | --- | --- |
| 比较什么 | **字面**——词有没有重合 | **意思**——语义像不像 |
| 怎么做的 | 数词、算权重 | 把文字变成一串数字（向量），算距离 |
| 需要什么 | 什么都不需要 | 一个向量模型 + 一个向量库 |
| 能离线跑吗 | ✅ 能 | 通常不能 |
| 能解释吗 | ✅ 能，能指出是哪个词命中了 | ⚠️ 较难，只能给个相似度分数 |
| 同义词 | ❌ 完全不认 | ✅ 认 |
| 换个说法 | ❌ 可能就找不到了 | ✅ 一般还能找到 |

**举个词法检索必然失败的例子：**

> 用户问：「**英文时态**有哪些用法？」
> 资料里写的是：「**现在完成时**的四种常见用法」

这两句话意思高度相关，但字面上一个字都不重合（「英文时态」和「现在完成时」没有共同的两字组合）。词法检索**找不到**，向量检索能找到。

### 那为什么第一版还做词法检索

- **不需要任何额外服务。** 不用装向量数据库、不用调 embedding 接口、不用申请第二个密钥。
- **不花钱、能离线。** 33 道验收题跑一遍是毫秒级，可以随便改、随便重跑。
- **可解释。** 分数不对的时候，能明确说出「是因为这个词命中了」。这比对着一个相似度数字猜原因要容易调试得多。
- **它是很好的对照基线。** 以后上了向量检索，可以拿同一套题对比：到底是变好了还是变差了？有了基线才知道。

### 加了一条「来源多样性」的限制

**这是实测逼出来的，不是一开始就想到的。**

纯按分数排序时出现过这样的情况：

```
问题：学过的单词老是忘，是不是我的学习方法有问题？

排序结果：
  9.28  course_faq.md        → 学习方式
  6.98  course_faq.md        → 效果与反馈
  6.02  vocabulary_study_method.md → 四个被验证有效的方法
```

取前两名的话，两份都来自 `course_faq.md`，而**真正讲学习方法的 `vocabulary_study_method.md` 被挤到了第三位**。

答案明明分散在两个文件里，却只检索到一个——这正是「该引两份，只引到一份」这种错误最典型的成因。

所以加了一条限制：**每个文件最多贡献 `ceil(top_k / 2)` 段**。

| top_k | 每个文件最多几段 | 效果 |
| --- | --- | --- |
| 1 | 1 | 没影响，本来只要一段 |
| 2 | 1 | 强制覆盖两个来源 |
| 3 | 2 | 既保证覆盖，又允许同一份多给一段 |

**代价是什么：** 如果某个问题的答案确实全在一个文件里，这样做会掺进一段别处的、相关性更低的内容。

这是**覆盖度**和**精度**之间的取舍。对问答场景来说，宁可多给一段略偏的内容，也不要漏掉真正相关的那个文件——漏掉是「答不上来」，多给是「稍微啰嗦」。

---

## 五、这一层现在能证明什么

已经用 `evals/rag_cases.json` 的 33 道题实测过：

| 项目 | 结果 |
| --- | --- |
| 索引只包含 3 份内容资料，**不含 README.md** | ✅ |
| 每个片段都带 `source` / `heading` / `text` | ✅ |
| 17 道单来源题，**top_1 全部命中期望来源** | ✅ 17/17 |
| 3 道跨来源题，**top_2 全部覆盖两份来源** | ✅ 3/3 |
| 空查询、纯标点、完全无匹配的查询 | ✅ 不崩溃，返回空列表，不硬凑 |
| 同样的查询问两遍，结果完全一致 | ✅ 可复现 |

**能证明的：**

- 切分逻辑是对的，出处信息完整保留
- 对于**用词和资料比较接近**的问题，能准确找到该看哪一段
- 说明文档不会被误当成依据
- 整个过程不联网、不花钱、可重复

**还不能证明的（很重要，别误以为 RAG 已经做完了）：**

- ❌ **不能证明「回答正确」。** 这只是**检索**，还没有把检索结果交给模型生成答案。检索对了不等于答得对。
- ❌ **不能证明能处理换一种说法的提问。** 上面那个「英文时态 vs 现在完成时」的例子就是现成的反例。
- ❌ **不能证明拒答能力。** 题库里 13 道拒答题，靠的是模型看到检索结果后判断「这里面没有答案」。检索层本身判断不了——它只会返回「最像的几段」，哪怕那几段其实答不了这个问题（实测中，13 道拒答题全部检索到了某些片段）。**拒答是下一层的事。**
- ❌ **没有评价检索的「精确度」。** 现在只看了「该找到的有没有找到」，没看「有没有混进无关的」。
- ❌ **资料规模很小。** 只有 3 份文件、16 个片段。真实场景可能是几千份文档，那时候对索引和性能的要求完全不同。

---

## 六、代码在哪

| 文件 | 内容 |
| --- | --- |
| `retriever.py` | 检索层本体。分词、切分、建索引、检索 |
| `test_retriever.py` | 自动化测试，对照 33 道验收题 |
| `evals/rag_cases.json` | 验收题库 |
| `knowledge_base/` | 被检索的资料 |

跑测试：

```powershell
cd C:\Claude-Code\ai_tutor
python -m unittest test_retriever -v
```

手动试检索效果（会进入一个简单的交互模式）：

```powershell
python retriever.py
```

---

## 七、下一步

1. 写 `rag.py`：拿检索结果 + 问题，让模型**照着资料回答并注明出处**
2. 拿 33 道题跑完整评测：既看检索对不对，也看「不编造」和「拒答」对不对
3. 之后再考虑升级成向量检索，并和现在的词法检索做对照
